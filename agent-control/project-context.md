# Reviewed project context — 2026-09-15

This is a deliberately small project snapshot supplied to the API. It does not
contain private sessions, the full project memory, absolute paths, or source code.
The local PROJECT_MEMORY.md and current user instructions remain authoritative;
refresh this snapshot before planning against later milestones.

- Formal name: Magic 600 Cell (historical name C600 Studio).
- Python model/backend plus Windows Native WinForms/MPUlt/DirectX interface;
  isolated browser experiments are internal evidence only.
- Full model: 177120 pieces, 259800 labels, 35 moving orbits, 1200 generators.
- Preserve canonical IDs, mappings, persistence and one Session/journal authority.
- Main workflow: Orbit First Block Building Solving, keyboard-first abstract hub,
  block building, Macro Base, buffer/insertion/orbit protection and endgame orientation.
- Piece labels retain IDs. Orbit presentation uses only verified mathematical meaning.
- Current, explicitly locked Next, hover and protection have separate semantics.
- G1 Grip/Twist and G2 abstract hub: separate runnable native samples and separate
  user approvals required; both Pending at this snapshot, no production integration.
- The source has existing uncommitted development changes. Local staff must define
  an explicit baseline and file ownership before assigning write worktrees.
- Native/GPU/human acceptance must not be inferred from source or browser tests.

Task-specific evidence must be explicitly provided in a reviewed task file. The
API controller never automatically uploads the repository or personal sessions.
