# Engineering team deployment specification

Scope: apply the supplied 33-page team proposal to Magic 600 Cell. Deliver project-discovered roles, orchestration rules, isolated-worktree support, a common verification entry, source-repository CI and an authenticated Agents API planning controller. The document's SaaS/authentication examples are examples, not application requirements.

Acceptance: seven roles with valid skill metadata and project agent definitions; local Codex accepts configuration; scripts reject missing inputs and retain nonzero failures; API controller has installed SDK and offline request tests; source repository receives only the infrastructure allowlist; existing application edits remain intact; manual credential/remote activation steps are documented accurately.

Exclusions: implementing the next UI, approving G1/G2, automatic solver behavior, release, moving personal sessions, enabling paid unattended API loops, or claiming GPU performance. API completion requires a successful authenticated run; local tests do not substitute for that.

The source worktree includes ongoing unpublished changes and ignored experiments. Deploy additive tooling in place; do not commit, stash or clean unrelated development. Persistent local paths and API state are ignored by Git.
