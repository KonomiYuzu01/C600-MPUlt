> Archived development document, published 2026-09-18. This is a stopped, unreleased 0.4 snapshot, not a new instruction to resume work. Historical status and failed results are retained; the current status is in the [progress index](../../../README.md). Local paths are anonymized; raw evidence and runtime files are not bundled.

# P1 native consolidation inventory

This is experimental UI consolidation, not production integration. G1 and G2 are independently pending user approval.

| Previous capability | Canonical replacement | Unique behavior retained | Removed duplication | Verification |
|---|---|---|---|---|
| Independently fitted block-cell thumbnails | One shared block/Target hosting-cell projection, with compact A/B preparation regions | Exact fixed P, actual I, shared vertex/face incidence and explicit object menus | Independent block thumbnail layout; small structure image helper remains necessary for buffers | Focused shared-coordinate, identity, forecast and hit-target fixtures; current whole-window run is recorded separately |
| Permanent Local / Global / Puzzle header buttons | On-demand linked native views through retained commands and F7/F8 | Native renderer, structure navigation, exact selection and return to main work | Repeated main-header view buttons | Native workflow opens Local and returns; dense-view evidence remains build-specific |
| Permanently expanded operation controls | One expandable Prepare/Macro/Cleanup region via Ctrl+R and explicit macro insertion | Finite phase editing, Check, Preview, Execute, Cancel, New/Reuse and keyboard routes | Persistent form area; no second operation draft or execution route | Two-insertion native workflow and lifecycle tests |
| Separate G2 protection summaries in graph and operation row | Persistent graph-owned scope and complete-operation status | Offscreen orbit/position requirements, net versus prefix results, full details | Second G2 summary label; G1 still needs its own summary in its separate harness | Inactive-context protection tests and scoped GDI captures |
| G2 floating keyboard and modal command-key editor | One persistent Keyboard page in the graphical work region, with inline capture/edit | All20 Grip/7 Twist controls, pointer inverse, bank/macro slots, full command settings and explicit conflict handling; G1 remains standalone | G2 floating Form path and duplicate modal editor route; Operation and Keyboard never stack | Full Native220112413 assertions; staged-preview22061388; final223319311 focused compact/wide and legal forward/inverse checks. Exact source differences and final matching hashes recorded in the review log |
| Rebuilt onscreen keyboard on each mapping change | Reused physical-key controls with current canonical mapping data | Twenty slots, all seven Twists, extra bindings, inverse, macro slots and editable commands | Repeated control disposal/recreation for unchanged structure | Reuse fixture plus actual Shell context adoption; see native results, not only control measurements |
| Macro-body presence implicitly selected display scope | Explicit Macro body / All steps view using the same selected fixed record | Full-effect review, fixed action inspection, stale withdrawal and canonical cycle endpoints | Clearing/reselecting a macro merely to recover its body view | Explicit scope request, stale/full mismatch and forecast-scope fixtures |
| Generic expression-only filter dialog | One F5 Piece Filter tool with typed identity/position presets and explicit Apply/Cancel | Existing predicate grammar, complete installed rules, replace and supported Boolean composition | Old F5 dialog branch; no new filter state or second parser | Twelve focused model tests, eleven tool checks and the final native workflow |

The experimental `ExperimentCellView.cs` contains the retained cell-view implementation plus scoped filter presentation and mathematical orbit captions. Only the experiment's compilation list uses it. The production `native/NativeCellView.cs` is unchanged (SHA-256 `fb53559ad157f1fd2a6131875dd26484acfa430696431203eb2dfE8a4aa9d75b`, case-insensitive). Its geometry and canonical cell picking are retained; per-cell sticker counts are not represented as per-piece geometry.

No immutable assets, Session authority, personal data or production launchers were removed. Broader redundant inspection/filter/template tools remain subject to the architecture's later verified replacement work; this table does not claim that work is complete.
