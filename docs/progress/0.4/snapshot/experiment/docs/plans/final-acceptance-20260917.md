> Archived development document, published 2026-09-18. This is a stopped, unreleased 0.4 snapshot, not a new instruction to resume work. Historical status and failed results are retained; the current status is in the [progress index](../../../../README.md). Local paths are anonymized; raw evidence and runtime files are not bundled.

# Final native acceptance and delivery

Goal: complete the accepted 0.4 scope on the existing Session/native implementation.
Specs: architecture S14/S17-S19; naming/recommendation 07; integration/endgame 08.
G1/G2 approved. This plan neither grants publication nor changes solving semantics.

1. Root: run startup/layout prerequisite; correct superseded native test expectations.
   Combine current focused suites and add 32 continuous legal mixed work cycles with
   at least 10 genuine fixed worksheet reuses, bank changes and fresh reviews.
   Assert exact identities/state, stale permissions, selected goals and protection.
2. Independent solver and keyboard reviewers: identify acceptance gaps and actual
   workflow defects. Root owns shared GUI/builds and all product fixes. Each fix
   needs a reproducer, bounded correction and affected regression rerun.
3. Inspect actual screen layouts, glyphs, focus/held keys, small windows and supported
   scaling; measure real input and rendering stages with disclosed environment.
   No screenshot, source or synthetic timing substitutes for missing evidence.
4. Packaging owner: isolated allowlisted frozen package, installed DirectX loader,
   immutable assets and pinned invariant evidence; no personal files or Microsoft
   redistributable DLL copying. Root tests the packaged native launch separately.
5. Freeze final product; run required complete native/core/lifecycle acceptance.
   Update dependency-bound evidence, current gaps and English delivery documents.
6. Record actual native solver sequence. Concise captions: action, feedback, result.
   Retain source footage and edit map; label cuts/speed changes; subtitles must not
   cover graph/puzzle/keyboard/protection. Decode and visually inspect final film.
7. Final package + hashes + compatibility/rollback + change-only release notes.
   Public release awaits explicit publication authorization after files are ready.

Current owners: root = native harness, UI, integration and recording;
geometry_frames = final headless regression; identity_solving = isolated timing
instrumentation and failed-fixture review; native_solver_review = independent
native solver operation and source review. Current GUI leases are in HANDOFF.md.
No agent may edit another owner's files or operate the shared GUI concurrently.

Latest user steering: after this current validation cycle, assign an independent
verification-only agent actual native UI ownership for representative continuous
solver slices, not merely existing scripts/source checks. Include Resume/scramble,
linked geometry/names/cycles, keyboard/frame/focus, macro/reference/review/execute,
protection recovery, cross-orbit Next/drafts, orientation and A/B residuals,
undo/redo/cancel/save/reopen, exact completion. Use legal sources and applicable
C2/C5/D5/A5 cases. Record build/source, minimal steps, expected/actual, solver cost,
severity and screenshot/log; distinguish defect/usability/preference. Root fixes
reproducible blockers then verifier independently rechecks. No human-trial or full
solve claim. This is a final-delivery gate, not a new architecture/workbench.
