> Archived development document, published 2026-09-18. This is a stopped, unreleased 0.4 snapshot, not a new instruction to resume work. Historical status and failed results are retained; the current status is in the [progress index](../../../../README.md). Local paths are anonymized; raw evidence and runtime files are not bundled.

# Paired reply / native Draw diagnosis

Read-only source review and extraction of existing evidence. No engine, GUI, renderer, benchmark or latency series was started. `report.json` hashes the inspected current sources and historical inputs; `analyse_existing_logs.py` reproduces only the log extraction. The historical Python profile uses older adapter/Session source and is not a current performance result.

## Observed boundaries

- Historical isolated five-sample bank case: command median 8.9 ms, phase 5.8 ms, cycles 8.1 ms, work snapshot 45.3 ms, Local payload 5.3 ms, native snapshot 20.5 ms, JSON 16.9 ms; total median 107.4 ms. Payload 569,399 bytes (work 360,808; phase 116,058; cycles 50,176). Component medians are not additive. No HTTP/C# adoption was included.
- Native 024123 timestamp brackets: two read-only keymap exports have median Draw 430.2 ms, of which the `exact hub objects` → `macro catalogue and keyboard` bracket is 396.5 ms. Five import checks: Draw 481.9 ms / same bracket 455.8 ms. Source/binary inputs were unchanged during that historical run.
- Native 011625 six bank changes: median Draw 999.0 ms / same bracket 473.2 ms; nine settings commands: 1074.7 / 569.2 ms. These older settings are not specifically all Local changes. Run had a later test failure; these intervals do not make it a successful acceptance run.
- The bracket is **not a Hub-only timer**: it includes `hub.UpdateContext` and subsequent Shell labels, tooltips, phase buttons, Local/Global status and center updates. Wall-clock logging/scheduling are included. No captured interval proves frame presentation or explains foreground changes.

## Three falsifiable repeated-work hypotheses

1. **Control reconstruction and synchronous layout dominate the native bracket.** `ExperimentShell.cs:231,248–280` calls complete Draw for every accepted pair. `ExperimentHub.cs:245–251,296–309` always rebuilds and disposes/recreates object controls. `DrawMacros` (`Shell:286`) calls `DrawWorkspace` in G2, followed by another explicit call at `Shell:280`; this duplicate is source-confirmed, its cost unmeasured. `RenderPhysicalKeyboard` rewrites key labels/accessibility/tooltips and measures rows even when its existing window is hidden (`Keyboard:109–127`). Instrument Hub.UpdateContext/Rebuild, the intervening context updates, each DrawWorkspace call, keyboard/row fitting and final readouts separately, including invocation/control-create counts. Reject this hypothesis if those spans are small; do not infer their cost from the broad diagnostic bracket.

2. **A tiny settings change still rebuilds/serializes complete work and native state.** `adapter.py:782–829,1789–1807` emits all library records and all banks, with complete context and Local data. Prior bank cProfile observed 72 `library_record` calls (28 ms cumulative), two `banks` calls (13 ms), 340 canonical serializations (38 ms), 53 filter `evaluate` calls (not 53 uncached filter evaluations), eight actual `Filters.styles` calls. `server.py:23–53` reconstructs 259,800 color/style/interaction arrays and hashes before deciding the wire delta is empty. Add per-component time and bytes, actual filter-cache miss counts, native changed-slot count and recipe/context hash counts. Measure Request bytes/read, JSON parse and ReadPair separately; LocalApi.Post polls with up to 40 ms sleep, so dispatch→Draw is not backend-only. Do not collapse distinct render/interactive styles, pending masks or profile-bound revisions.

3. **Forecast consumers repeat finite projection work for unchanged mathematics.** Every G2 Send attaches both phase and cycle requests (`ExperimentPhase.cs:33–40`, `ExperimentCycles.cs:63–71`). `snapshot(prediction=True)` directly calls uncached `Model.net` for a fresh Ready/Staged nonexecuted draft (`adapter:841–853`, `core.py:182–192`), despite existing effect caching elsewhere. `inspect_phase` can separately project Prepare and the selected boundary; After cycles separately construct a predicted PuzzleState (`draft_inspection:45–51,88–95`; `cycle_projection:143–153`). Current cycles reuse a matching published residual bundle; if none exists they derive another bundle without publishing it. Count and time net/effect-hit/PuzzleState.refresh/residual_bundle calls per single request, recording recipe hash and depicted state hash. Compare Current versus After, empty versus already-reviewed nonempty draft, and executed state. This is not evidence that all effect proofs or all 35 frames are always recomputed.

## Minimal next measurement

Once the sole GUI owner releases it, collect a short diagnostic slice first: fixed legal non-Home baseline, warm selected macro/draft, five alternating bank and five explicit Local-center requests, with identical window sizes. In a test-only captured-source build, add Stopwatch spans around transport/parse, ReadPair, UI-queue wait, Bridge.Apply, phase adoption, the Draw subspans above and RefreshCommandAvailability. Buffer records in memory and write outside timed sections. Temporary Python wrappers time the named backend calls without altering arguments/results, locks or cache behavior. Bind generated diagnostic copies to original source hashes. Do not change production behavior just to create a faster measurement.

For each sample retain canonical state hash/full labels, ReviewContext/guard, pending token, selected macro version, Current/Next/protection, actual bank/center/frame/filter and native snapshot revision/profile. A navigation request may retain mathematical context but must show the new key map or center. Never reuse a stale paint merely because the puzzle hash is unchanged. Error/recovery requests, cancelled requests and rejected input are separate rows. After mutation/undo/filter/roles/reference changes, forecasts must still withdraw and only matching fresh results may return.

The frozen `NativeLatencyChecks` already observes real adopted snapshot → CPU return from Present or actual WM_PAINT. Its 100/100/100 series is later acceptance, not this diagnosis; no GPU-completion, physical-key or quiet-benchmark claim follows. Correct complete labels, exact protection checks and single Session authority remain mandatory.
