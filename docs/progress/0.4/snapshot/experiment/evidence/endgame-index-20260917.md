> Archived development document, published 2026-09-18. This is a stopped, unreleased 0.4 snapshot, not a new instruction to resume work. Historical status and failed results are retained; the current status is in the [progress index](../../../README.md). Local paths are anonymized; raw evidence and runtime files are not bundled.

# Endgame evidence index update

The existing schema/checker is unchanged. The index retains all 26 older records
and adds 18 bounded records. No tests or native windows were operated for this
index update; hashes establish currency, not mathematical or user approval.

Current checker result (exit 1, expected because historical failures/gaps remain):

| Registered scope | Current status |
|---|---|
| Seven finite-family/catalogue checks | Passed |
| Ten proper-reference compilation checks | Passed |
| Original integration batch 235948 | Failed; commands 0 and 8 failed |
| Commands 1–7 from that batch, 64 tests total | Each passed in its exact scoped record |
| Candidate ten + reference API two, batch 001749 | Passed |
| Final 35 manual residual paths / 59 stages, batch 002047 | Passed |
| Core/reference/crash batch 234601 | Stale harness only; recorded successful execution retained |
| Native 001749 | Failed focus-caption assertion retained |
| Native 002202, 98 assertions | Recorded pass, currently stale after candidate tooltip/test edits |
| Native 002905, 104 assertions | Passed; all 123 captured immutable inputs unchanged |
| Reference matrix 003749, one test / 59 fixed recipes across 35 orbits | Passed; all 58 runner-captured inputs unchanged |

For 235948, seven `derived-subcommand-N.json` receipts were created beside the
original `result.json`. These are exact index-time extractions, not new runs.
Each binds the original failed aggregate's bytes, original result index and log,
plus the recorded source/model/harness and relevant test hashes. Unrelated test
files were excluded only after checking that the selected tests import no other
test modules. All other captured source inputs remain. The failed aggregate
has its own failed record; no record treats the entire batch as passed.

The core receipt's sole changed input is `tests/run_bound_checks.py`. Its three
product tests and underlying product/model inputs are unchanged; the index does
not substitute the new runner hash for the tested one or rerun tests merely to
make this record green. Final acceptance still requires its designated checks.

Native records retain captured source, model, toolchain, runtime origins and
immutable runtime identities. Mutable MPUlt settings stay explicitly described
by initial/before/after hashes inside the bound build receipt, not asserted as
unchanged inputs. The first focus assertion failure has no established root
cause. Later foreground-verified captures do not establish uninterrupted focus
ownership or a uniformly repaired focus condition. The coordinator's final
002905 run adds three editor-entry/cancel repetitions with matching active-form
and footer, unchanged work, and actual desktop evidence of the shortened tooltip.
Its executable SHA-256 is
`ecfce866afe0025a0f2e4899cc25ddb4522472b71f00248278d4fea92a3c94c5`.

At this update, current passed records exist in macro effects (2), protection
(1), recommendation (2), endgame (7), persistence (2), and native UI (1).
Naming/orientation records retain their historical/stale limitations.
`tests_executed=false` and
`final_acceptance=not-assessed` remain the checker results.

Read-only check: `python tests/validation_evidence.py --json`; output is
`evidence/endgame-index-check-20260917.json`.

The final reference matrix binds the existing C1-to-C7 all-1200-generator
certificate as an exact record-hash prerequisite. It checks complete 259800-label
net equivalence, canonical A/B/X/Y role maps and collateral effects for 59 fixed
recipes; the longest source/emitted words are 98380/98444 primitives. Its additional
captured toolchain/certificate dependencies bring the record to 70 input bindings.
It does not establish all-frame coverage, Strict prefix-protection permission,
native workflow or arbitrary-state solvability.
