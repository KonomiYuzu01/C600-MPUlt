> Archived development document, published 2026-09-18. This is a stopped, unreleased 0.4 snapshot, not a new instruction to resume work. Historical status and failed results are retained; the current status is in the [progress index](../../../../../README.md). Local paths are anonymized; raw evidence and runtime files are not bundled.

# Magic 600 Cell active handoff

## Current final acceptance — 2026-09-17

**RESUMED by explicit user instruction.** Root owns serial GUI access and product
integration. The user explicitly requested continued work despite progress/steering
messages. Native_solver_review now owns the single GUI lease on wave-d/build8da860;
normal owned-window activation plus fresh foreground verification is allowed,
never input into Codex or bypassing a cancellation. Wave-c was closed normally
after its immutable stop receipt; older39c8/9a8a windows are also closed. No scheduled report
or auto-resume is enabled. G1/G2 approvals remain recorded; final release is not
claimed or published.

Latest bounded evidence:
- Wave-d actual review build8da860: first56-turn E1 commit, locked Next promotion,
  undo/redo, whole-orbit conflict refusal and single-position protection observed.
  GUI lease remains native_solver_review; bounded second insertion pending.
- Final55 headless baseline is still running; original product/tests remain frozen.
  Fresh35-orbit/59-stage manual paths passed; several fixtures predate accepted
  Next/template/workflow contracts. Original failures remain evidence.
  Real defects: post-primary cancellation reply and missing-frame diagnosis.
  Candidate adapter and corrected fixtures under evidence/final-headless-resume-20260917/;
  not yet promoted or certified. S3 named protection picker candidate also remains copy-only.
- Host has8GB RAM and measured0.5–0.8GB free with baseline+native review. Root stopped
  only its own contract candidate worker, recording an incomplete result, then
  serialized further Model-heavy runs. Do not count that attempt as passed.
- Wave-c actual native review (7f0799dd89382712) reached E1, mathematical-name
  search, explicit Star0 reverse selection and Add. Check was refused before
  input because the owned window was no longer foreground. No execution or
  orbit completion is claimed. Session g2-solver-review-20260917-c was later closed normally;
  evidence/native-solver-review-20260917/wave-c/progress.json and the111-input
  unchanged stop receipt preserve the exact boundary. Never rebind this receipt
  to later source edits.
- Fresh core/reference/crash recovery: evidence/core-contracts-/20260917-054300/
  result.json passed all3 commands with unchanged inputs. Existing core report
  bytes were restored; this is headless evidence, not native acceptance.
- Fresh backend navigation profile: bank median88.44ms, Local77.30ms on legal
  [1,3] state, full paired replies and label/context invariants unchanged.
  evidence/performance-resume-20260917/backend-review/FRESH_PROFILE.md.
  This does not explain the full native Send delay; no backend product rewrite.
- Keyboard candidate was independently reviewed, promoted with exact hashes and
  passed actual native060123:104 checks/134 unchanged inputs. Independent review:
  docs/reviews/keyboard-batch-independent-review-20260917.md. Full G1 and performance
  remain pending. Frozen copies/provenance/promotion live at
  evidence/performance-resume-20260917/keyboard-review/candidate.
  Root owns integration and tests/KeyboardBatchNativeChecks.cs;
  geometry owns final headless regression; identity owns copy-only API timing;
  native_solver_review owns independent actual Solver review. No GUI input authorized
  to a second agent concurrently.
- Independent actual native wave-b (build9a8a8e39533dc179): E1 loading,
  Current/Next, macro search/filter/select, explicit Add, Check and Cancel.
  It did not execute a solution or complete an orbit. Two discovery problems
  were reproduced: current-orbit mathematical-name search failed before effect
  analysis, and an empty checked-use filter concealed the Check library route.
  See docs/reviews/native-solver-review-20260917.md and wave-b-index.json under
  evidence/native-solver-review-20260917. All111 bound inputs stayed unchanged.
- Display native050049:72 assertions passed,133 inputs unchanged. Real slider
  mappings, rejection/busy feedback, renderer preferences, focus and Reset
  geometry synchronization were checked. Native mapping witness matches all
 259800 labels/1200 generators. Earlier045817 is a failed harness-boundary run,
  not a product pass. See native-baseline/postapproval-g2-20260917-050049.
- Instrumented native050237:52 assertions and20 complete command traces passed
  correctness checks. Five bank switches took1.76–2.02s; five Local switches
  took1.62–1.97s. This ran concurrently with the frozen checker: diagnostic,
  not quiet performance acceptance. Hub rebuilding and repeated keyboard/focus
  rendering are measured large costs. See its native-draw-timing.json and
  timing-validation.json. Do not add inclusive nested spans.
- Package candidate packaging/artifacts/20260917-044351: actual frozen payload
  and all7 non-GUI groups passed (check-full-20260917-050328). Legal engine
  commit/undo/reopen, logs/keymap copy compatibility, runtime mapping and cleanup
  were exercised. No frozen GUI launch, historical-profile migration or human
  full solve is claimed. Subsequent product edits require a new final package.

Macro discovery corrections passed native052216 (65 assertions,133 inputs
unchanged). Root and independent reviewer inspected the true desktop and minimum
Solve GDI images; Check/Edit wrap visibly and phase/execution/context remain
exposed. S1/S2 are closed for fresh E1 without prior macro selection. Three-line
frame-unknown text and the selected-but-hidden branch were not exercised. See
docs/reviews/native-macro-discovery-plan-review-20260917.md and the independent
macro-discovery-052216-review.json. No automatic selection/analysis/execution was added.

Measured performance correction: geometry_frames owned only ExperimentHubCycles.cs
and batched the two ComboBox updates, keeping full filling/selection and finally
restoration of guards. No cache, skipped effect checks or KeyHint refactor.
Root built product7f0799dd89382712 and ran the same copied-source diagnostic052827:
52 assertions,20 complete traces, unchanged inputs. Compared with051917, measured
five-sample bank median1960→1768ms and Local1281→1238ms; Hub internal reductions
are clearer. Concurrent workloads remain uncontrolled and an inclusive warmup
comparison regressed, so this is partial diagnostic improvement, not S14 acceptance.
Current/Operation/After, nonempty preview and actual selector navigation still need
affected cycle/focus regression. Detailed evidence: hub-review/FINE_TIMING_051917.md
and its before/after comparison. Tests own disposable sessions; all runs exited.

Current ownership: root integration/GUI; both reviewers have completed their bounded
work. No queued product edit. Validation index retains the captured old hashes and
marks earlier evidence stale when HubCycles or another dependency changes; never
backfill old successful receipts with current hashes.

Next: reduce remaining keyboard/availability refresh cost using measurement;
affected cycle/stale/focus regression, then continuous32/16, complete native latency/
layout and final regression, fresh package/native launch, captioned film and
release authorization. Engineering estimates remain whole0.4 about86%, final
round35%; these are not pass rates. Final filming and research-update requirements
remain in PROJECT_MEMORY: compact action/feedback/result, disclosed cuts/speed,
subtitles clear of mathematical views; release notes only genuine changes, and
research report updated after formal release. Existing API workflow stays in scope.

Earlier compatibility context (valid only for unchanged dependencies):
Log/native mapping and keymap file routes are connected; see
`docs/reviews/final-compatibility-20260917.md`.31 focused Python checks and4
post-import failure-boundary checks pass. Native023401 log sub-result passed,
but aggregate failed at keymap focus; corrected fixture024123 passes16 assertions
with unchanged inputs and actual desktop capture. Do not count the failed
aggregate as passed. Its capability matrix distinguishes new actual use
from inherited evidence and uncovered scope; do not rerun unchanged retained
features without a changed dependency or a concrete concern.
Retained observation audit: instant TRate=1 and framework/adaptive renderer remain;
the missing frame/adaptive/size/FOV/light routes are restored through the command
registry and compact Display window. Rejected slider feedback and Reset dimension
synchronization are corrected. Bound headless checks: 23 passed in
evidence/display-controls-20260917-042026/result.json. Native follow-up passed
at050049 as recorded above; build3c71 predates the last Reset quantization fix.
Instant TRate=1 is
unchanged. Do not rerun unrelated retained renderer checks without cause.
Packaging checker: 17 bound checks passed in
evidence/package-acceptance-20260917-042338/result.json; independent preservation
recheck 5/5. See docs/final-package-notes.md and
docs/reviews/final-package-checker-review-20260917.md. Actual frozen checks and native
Geometry witness are now recorded above; legacy-shaped fixture is not historical migration.
Screenshot assertion triage: VulcanMessageLib.node / CCX-Process identifies an
Adobe component, not a proven Magic crash. Magic PID25540 responds; no matching
Application error found. Exact initialization cause unknown. Read-only evidence:
evidence/runtime-assertion-triage-20260917.json. No Adobe/runtime changes made.
ComputerUse screenshot reports SetIsBorderRequired E_NOINTERFACE; element clicks
lack geometry and main-window targeting misses the modal. Bounded test-only
SendInput helper is pinned to the owned sample; no security denial is bypassed.
Review evidence: docs/reviews/native-solver-review-20260917.md. The bounded wave-b
slice is recorded above; it is not a complete solve.
Next: independent actual-UI solver slices, continuous32/16 reuse, latency/layout,
full final regression/compatibility, package, subtitle film, publication approval.
Do not schedule auto-resume. No final accepted package or new film yet.

Plan: `docs/plans/final-acceptance-20260917.md`; evidence entry:
`docs/reviews/final-solver-acceptance.md`. U25 exposed a real integration gap:
Session/New/Resume/scramble/timer/completion controls were not connected to the
new native shell. The existing Workflow is now injected into the adapter;
Session.reset has an atomic validated preference/attempt hook, with 11 helper
checks and fresh core/reference/crash passes. Native Session85 checks passed in
`native-baseline/postapproval-g2-20260917-020650/`, including focus/camera/deferred
completion. Audit found missing native log import, MPUlt export forwarding and
keymap-file routes; those now have focused compatibility evidence linked above,
with12new+11retained log-helper checks passing. Broader final regression remains
pending. Reset workspace preserves block and
orbit protection. New retains personal banks/macros/sheets and a recovery checkpoint.

`tests/final_workflow_cases.json` supplies 32 legal mixed cycles and 16 actual
sheet reuses; continuous native acceptance is pending. The D5 five-stage legal
cleanup has separate full-label backend proof. Keep failed native baseline
records: test-only scroll/Help/key-order assumptions were corrected, clipboard
restore0x800401D0 has a10-check bounded contention fix; full native replay remains pending. Packaging
scripts and subtitle/edit-map tooling exist but no final package or new film
has been produced. Only root operates the shared native window.

User cancelled the 15-minute report automation (`magic-600-cell-15`, paused).
Current estimates: overall0.4 about85%; this final acceptance round about30%.
These estimates do not constitute verification or release readiness.

After this validation cycle, an independent verifier must operate native solver
slices manually through actual UI, beyond existing scripts/source checks. Serialize
GUI ownership; verifier reports/reproduces/rechecks only, root implements fixes.
See final acceptance plan and shared steering queue; no human-trial/full-solve claim.

## Latest cycle — 2026-09-17, manual endgame integration

Continue from the current experiment; do not rebuild the solver or create an
endgame workbench. Existing Solve/Macro Base now exposes explicit known families,
reference variants, candidate comparison, residual details and journal delta.
Position-only/exact Net/Strict protection and atomic Session context transitions
are integrated. Same-orbit checkpoint macro-selection mismatch was reproduced
in native and fixed before effect/forecast adoption.

Evidence and failure history: `docs/reviews/endgame-integration-20260917.md`.
All35 chosen legal paths /59 stages pass with earlier ORDER orbits explicitly
protected; four unfinished orientation recovery cases cover C2/C5/D5/A5. Current
bound receipt: `evidence/endgame-integration-/20260917-002047/`. Finite299 witnesses
and1202 later-only collateral edges are separate from universal solvability.
Native `postapproval-g2-20260917-002905` passes104 checks,123 inputs unchanged,
including restored selection→Add, reference inspection, candidate ordering,
cross-orbit Next/return, compact Solve and repeated tool focus/cancel.
The earlier isolated focus-assertion failure has no proven root cause; keep its
record and repeat transitions during final continuous native acceptance.

The last bounded integration check also passed:59 representative endgame recipes
across35 orbits compiled into explicit C1→C7 proper frames, with full259800-label
equality and canonical role/collateral checks. Receipt
`evidence/endgame-integration-/20260917-003749/`, detailed
`evidence/endgame-reference-matrix.json`; longest98444 emitted primitives. This
does not certify every possible reference or inherit Strict protection.
No known functional blocker remains in these scoped paths. Remaining work is
continuous native acceptance/scaling/latency, final isolated packaging,
compatibility and concise solver recording. No intermediate package or public
release. Entire expanded0.4 estimate80–85%; this round85–90%, not acceptance rates.

The older sections below are historical handoffs; their missing-feature statements
are superseded by the latest cycle and its evidence.

## Latest cycle — 2026-09-16, complete-operation CurrentScore

Selected complete operations now publish endgame-aware CurrentScore from the same
ReviewContext, residual pair and full effects. Reasons carry canonical actual-before
objects, including first-prefix protection conflicts even when the net action is
identity. Solve Protection links those reasons to the existing Operation graphic;
inspection temporarily hides Solve, with the same page/draft available on return.
Fixed inspected positions have no invented cycle arrow. Functions toggle and editable
single-key reason routes coexist; key hints use the onscreen keyboard formatter.

Final native `postapproval-g2-20260916-223353` passed54 agent-operated checks with
unchanged source/model/runtime hashes and actual1920x1080 desktop captures (native
1280x720 logical workspace). It includes compact Solve, stale withdrawal, canonical
evidence, explicit preview/commit/undo and Strict rejection of legal net identity.
Earlier white desktop surfaces did not recur with the UI message loop active during
capture; their old cause is not claimed resolved. Existing oversized object hover
text was shortened after actual-screen obstruction; full accessible details remain.
See `docs/reviews/current-score-integration-20260916.md` and validation index.

Next limiting factor: usable endgame protection/intent and explicit reference paths,
then all35-orbit continuous coverage and dynamic library applicability. The helper
does not complete library-wide candidate ranking or position-only protection APIs.
No automatic search/selection/execution added. Entire expanded0.4 estimate65% (60–70%),
not acceptance coverage. G1/G2 approved; no intermediate package or release created.

## Active integration — 2026-09-16, naming / cycles / coherent orientation

Latest authoritative additions are the desktop project's 07_NAMING_AND_RECOMMENDATION
and 08_INTEGRATION_AND_ENDGAME. User approved the u0/u1 local representative repair;
it preserves the same structural classes and canonical model identities. G1/G2 remain
approved for a156, not a declaration that new work is complete. No intermediate
package; final continuous solver recording must be compact, honestly mark accelerated
or omitted waiting, and preserve operation → feedback → result. Subtitles avoid the
puzzle, cycle graph, keyboard and protection readouts.

Implemented and headlessly checked: exact joint Host/M structure names, all433
reference-cell addresses (42 local A4 components), all177120 fixed-position address
round trips, typed Piece/Position/Slot parsing, six classes with u0/u1. Existing IDs
and saves remain unchanged. Eighteen naming checks and six input-integration checks
passed on the real model. Native name-copy/input run195547 passed after the fixture
detached its clipboard backup; earlier194436/194853 OLE clipboard failures are real
failures, and their exact owner/provider cause is not proven. Application Copy code
was unchanged in that replay. Do not mislabel a test-harness change as an app fix.

The new operation-cycle graph replaces the coarse triangle projection. It pages
complete exact cycle support, stationary orientation changes, slot correspondence,
actual occupants versus after-Prepare occupants, and retains canonical selection.
Root fixed empty-cycle negative indexing (reported IndexOutOfRange/red-X), lost first
selection after a draft change, orbit override and page cursors. Native193609 passed
29 focused assertions with a real desktop capture; previous193339 ring-focus failure
requires continued attention. Full195900 passed592 native assertions, but later edits
invalidate its current-build applicability. Its desktop cycle capture was obscured by
owned tool windows; the new fixture hides them for the graph capture. Product multiwindow
obstruction remains open. Sequence and macro hover text are now concise; details remain.

TransportedFrames now drives adapter classification, cycle frame correspondence and
the07 deterministic UseScore. Existing all1200/full259800 reconstruction evidence is
retained; a narrow immutable finite-group accessor was added for the new invariant
audit. ReviewContext binds model/naming/frame, state, roles, Next, recipe provenance,
phase boundaries and full protection. Headless checks: review context7, cycles14,
phase10, macro-use18, contract35 (one optional engine smoke skipped).
Independent review reproduced sticky incomplete frame caches and cancellation bypass
on cached analyses. Seven red regressions now pass after explicit retry/summary refresh
and cancellation checkpoints; passive browsing still does no analysis. Native203841
and focused204321 failed foreground ownership before capture; focused205254 passed32
without an application focus fix. The intermittent earlier failure remains unexplained.

ResidualState is now integrated through the existing adapter and shared ReviewContext.
Replacing an external pending preview rechecks changed context and restores old work on
cancellation/failure (24 targeted checks). Current/Operation/After share the existing
graph; no new workbench. Twelve residual graph checks plus14 retained Operation tests
passed. Native211507 passed54 assertions, including fixed visible mode controls during
compact scrolling, no false After tokens when prediction is unavailable, and canonical
selection. Its matching build manifest reports unchanged inputs. Screenshots include
an actual1920×1080 desktop and1280×720/1000×650 GDI windows; the process reports96dpi,
not evidence of another monitor or manually changed Windows scaling.

User requested T⁻¹ typography. Display/copy changed; ASCII T^-1 canonical words and old
inputs remain. Focused433 roundtrip/new-old typed input plus6 adapter input tests pass.
See evidence/math-glyph-20260916-211106/REVIEW.md: Windows fallback renders ten primary
Segoe UI cmap gaps; do not falsely declare those absent onscreen. Current graph math
symbols were inspected natively, but all-window/scaling acceptance remains outstanding.

WorkIntent helper is integrated into adapter review and worksheet schema2 validation.
Home goal is explicit; old capture remains. New10 real-model checks and41 related old
checks pass. Native214529 passed35 bounded assertions for the goal picker, Home-goal
action, Operation-set routes, separate goal outcome, explicit commit and exact undo.
Native214323 passed17 name-copy/editor checks including T⁻¹. The Solve role buttons
now use Button.GetPreferredSize: former54px clipped the whole mathematical address,
native60px has the same suffix pixels as the78px reference (role-text-20260916-213632).
GDI and actual desktop214529 show those addresses; other actual desktop controls are
white. That is not visual acceptance: capture blocks the UI thread after DrawToBitmap;
pending paint versus capture side effects remain unproven. Probe214921 was interrupted
by foreground switching and captured another app, so is unusable for visual comparison.
The harness now checks the actual Windows foreground HWND before and after capture;
this last change is compile-only, evidence/typography-build-20260916-215301. No production
focus/renderer fix is claimed. See docs/reviews/capture-focus-20260916.md before the next
controlled native capture. No further uncontrolled replays. Dynamic CurrentScore, runtime invariant
certificate binding, completion transitions and complete endgame workflow remain open.

Read docs/reviews/endgame-evidence-audit-20260916.md for ten fresh replays of saved
historical legal endgame witnesses. D5 final-B has22 collateral orbits; A5 final-B
expands to98372 primitives and affects O33 as well. This fits existing numeric limits,
but native responsiveness/prefix protection and35-orbit workflow coverage remain.
No automatic target construction, setup search or parameter selection was run.

Current ownership: root final integration, exclusive native builds/GUI and shared docs;
identity_solving bounded adapter/worksheet intent changes are frozen; geometry_frames
maintains evidence bindings; workspace_keyboard reviews native rendering/capture evidence.
The new finite-group invariant audit passed all1200 legal generators across35 orbits;
its runtime integration remains pending. Endgame extends the existing Solve/graph,
not another workbench. All application
changes remain in the isolated experiment except previously documented shared grip
compatibility support. Preserve existing dirty work, immutable assets and personal data.

## Resumed — 2026-09-16 solving-use classification and continuous Solve

The user explicitly resumed development. The stopped entries below are historical.
Current plan: `docs/specs/solve-use-stage-20260916.md`. Root owns native GUI, Shell,
classification presentation, builds and documentation; identity_solving owns backend
classification/adapter; workspace_keyboard owns window consolidation; geometry_frames
owns reference discovery only. Preserve all existing dirty work and personal sessions.

Latest user clarification: affected orbits, Star, pure cycle and orientation are
inputs to an inferred **usual solving-use** classification, not the final categories.
Multiple orbit uses and Other are allowed, with reasons. Mathematical proofs and
current suitability remain separate. Explicit reference variant, post-insertion
Next/candidate focus and solved-transition protection permissions are recorded in
architecture S00; none has yet been claimed implemented. Pure cycle requires an
explicit independent reference on each edge, not merely eventual frame return.

API durable session has received bounded `solve-use-review-20260916.md`; no new
agent/session or runtime API classifier was created. Its context was refreshed to
approved G1/G2 and current permissions. Figma existing study and available fonts
were inspected; a design study is not native evidence. Full follow-on obligations
in TOMORROW_COVERAGE remain active. No intermediate package or release.

## Stopped for the day — 2026-09-16

User requested one bounded milestone tonight, then stop; wait for their next reminder. No new timer or wakeup. G1/G2 remain explicitly Approved for a156; later P2–P6 development is authorized. Do not restart or treat unfinished features as approved/completed.

Completed tonight: explicit Macro details comparison, independent inverse / chosen legal-word R^-1 / Macro / R variants, preserved source metadata provenance and explicit Select → Add → complete protection → preview/commit → fixed worksheet reuse. New variants do not inherit misleading forward tags, purpose notes or pins. Shared Macro routes are appended, and Help explains scope. R is not a certified spatial-reference map. A global Stop-acknowledgement barrier prevents a late cancellation from spilling into the next command; saving retains its authoritative outcome.

Fresh final native runs: G2 `postapproval-g2-20260916-085608` exit0 /339 assertions, G1 `postapproval-g1-20260916-085932` exit0 /168. Both 45-file manifests match the final current application/backend/fixture sources. New real-model tests12 and existing library regression14 pass. Fresh retained core/reference/crash and NativeHost/layout pass. Exact hashes, first-run failure and correction, inspected screenshots and evidence limits are in `evidence/macro-variants-20260916/REVIEW.md` and `native-verification.json`. These are Agent-operated/injected native fixtures, not human solving trials. The deterministic late-Stop fixture uses controlled transport/busy state; failed-ack lockout was source-reviewed, not independently fault-injected natively.

Next session must read the desktop project's `outputs/magic600-next-session/START_HERE.md` and `docs/reviews/TOMORROW_COVERAGE_20260916.md` here. Tonight's connection is complete within its spec; full reference/endgame, all35-orbit/residual coverage,30 continuous cycles/10 real sheet reuses, small-screen window obstruction, actual adoption-latency diagnosis, optional single-renderer Actual Puzzle detach, final consolidation, packaging and continuous solver recording remain. Choose the next largest demonstrated workflow obstacle. Research material updates follow formal application release; a complete verified migration backup follows actual0.4 completion. Neither is triggered by tonight's milestone. No intermediate package or public release was made.

All native test processes are closed and application source is frozen for tonight. Root owns only final handoff/evidence updates. Historical entries below retain their evidence dates and do not override this status.


Latest user requirement: final Keyboard must be compact. Implemented default880×490 (was1056×540), unchanged font, five primary rows, explicit modifier-key disclosure, custom unmodified keys always available, full focused/active cap and verified frame mapping. Backquote disclosure preserves old Grip/Twist assignments unless explicitly overridden. All current application/test sources are frozen; agents have released ownership. Root has closed the owned native test processes. Do not restart from the historical sample or reopen personal sessions as tests.

Fresh complete native evidence: G2 `postapproval-g2-20260916-081709` exit0/307 assertions; final G1 `postapproval-g1-20260916-082150` exit0/162. Final G1 manifest matches every current source. G2 predates only the Input Shift lifecycle correction and G1-specific fixture extension; final G1 +19 direct-router checks cover those changes.29 offscreen caption/layout checks,31 WorkReadout sizing checks,9 exact-model frame tests and earlier core/reference/crash wrapper `20260915T223149Z-ba82286e` passed. Assertions are not independent human scenarios. Evidence and source/build identities: `evidence/compact-keyboard-20260916/{REVIEW.md,verification.json}`. GDI controls versus actual desktop captures are explicitly labelled. No new native high-DPI/RTX4070 or human trial claim.

Resolved this cycle: Local/Hub filtered affecting-cap Grip feedback; physical press versus latch versus rejection; complete effect details now use the complete operation scope; detailed searchable Help; window-layout background ack no longer reserves foreground input; exact last-key clipping and narrow Macro/Help Next readout defects. WorkReadout's queued measured layout is limited to actual parent allocation correction. Stable mathematical names/identities, explicit operations, Session/journal and assets remain intact. Keyboard frame_bases is detached read-only display data from existing verified H/T tables, not new persistent identity.

Next high-impact work: simultaneous Keyboard/Macro Base placement on the1280×720 logical desktop still hides much of the central graph; inspect a normal complete task with cooperative tool visibility. Command adoption latency (~1.3–1.8s in prior sample, includes fixture padding) remains unresolved; `tests/profile_compact_transport.py` is prepared but has not run. Actual Puzzle detach remains an unapplied candidate patch requiring rebase and actual single-renderer/lifecycle tests. Remaining Macro Base relationships/reference/endgame and sustained30-cycle/10-reuse/all-orbit acceptance, final packaging and continuous native solver recording are incomplete. User does not want intermediate test packages. Public0.4 release still needs authorization.

Only retained source change this cycle: `grips.py` adds a backwards-compatible optional immutable rotation table to eliminate repeated NPZ decompression. All other application changes remain in this experiment. Build manifests now include that shared source and work_sheets.py/ExperimentWorkSheets.cs. Immutable assets, Session/journal and production launchers were not modified. The old historical blanket “all app changes isolated/frozen” statements below are superseded by this paragraph and current ownership.

Updated2026-09-16. **G1 Approved; G2 Approved by the user in the active development task.** Approval covers the a156 native sample and delivered recording, not unimplemented capabilities. Remaining P2–P6 development is authorized. Keep development data isolated, preserve production assets and personal sessions, and deliver a continuous solver recording when complete instead of intermediate test versions. Internal validation remains mandatory. The old heartbeat remains paused.

Latest direction: independent linked Local/Global/Keyboard and operation tool windows; retain fullscreen hub. Geometry is a faithful observation surface, including actual 433-sticker cells and transparent filtering. Mathematical display names distinguish identity, Home and current position while keeping canonical IDs. Explicitly selected visible single-key sets cover all operations; exact ordered-frame/keycap/graph/permutation correspondence and color-indexed Local center selection are required. Root owns integration and shared native GUI; bounded reviewers are inspecting geometry, windows/input and names/backend independently. Historical Pending statements below describe earlier evidence only.

Current cycle: new post-approval sources are in the same experiment directory; do not restart from a156. See docs/specs/post-approval-workspace-20260916.md. Native run postapproval-20260916-051840 passed53 internal assertions but failed normal shutdown (tool-window close cancellation); owned fixture process was terminated after normal close failed. UI review requires readable key captions/Current/Next/protection footer and much less dominant transparent Local context. Window/input and geometry owners are fixing these now. Root added full-label frame dispatch, goals/block reference, defaults and Next context recovery; nine boundary tests passed. Names helper has nine tests. API retry after user recharge actually completed in existing session. Next: freeze integrated files, compile, verify fixed lifecycle/UI and all-registry single-key coverage, then finish remaining macro/reuse/reference/endgame/consolidation and final continuous recording. No final acceptance or new release claimed.

Latest task: actual native workbench screen recording delivered in the desktop project's `outputs/magic600-workbench-recording-20260916/`. Combined video13:08.4,1920x1080, optional English captions/chapters and two raw segments. Full source records remain here in `recordings/native-20260916-035030` (main116 assertions) and `recordings/native-20260916-040228` (supplement35). Both use unchanged a156 application/backend sources with a separately identified paced in-process driver; injected scan examples are not physical typing. Full combined decode23652frames passed. Neither a human solve nor G1/G2 approval. Model/application sources and personal sessions preserved; all owned recording windows closed. See the recording README for coverage, hashes, tooltip obstruction, scaling and unfinished frame/reference/endgame limitations. Continue the existing implementation work from a156; do not restart or treat footage as a completed P1 checkpoint.

## Scope and authority

Actual checkout: `<source>`, HEAD `5e1d35de792ccc8db896b8abf74ff2b1b00e0749`. All current app changes are under `work/experiments/magic600-04`. Preserve unrelated dirty team/source files, immutable model assets, retained renderer and personal sessions. Current user direction is the spatial graphical instrument, with Keyboard/settings integrated in the abstract hub; Figma and Lucid are supporting design tools. No UI framework or rendering architecture was replaced.

Root owns final integration and native GUI. Keyboard reviewer released the focused workflow test; all agents are now read-only. Interaction reviewer inspects supplied GDI images without touching the desktop; solver reviewer performs source review. Do not infer user approval from any reviewer. All current application and test sources are frozen.

## Implemented keyboard iteration

- G2 has one shared bottom work region: Keyboard or Operation, never both stacked. The20 Grip/seven Twist controls retain actual router/bank mappings and cell palette. Input destination/phase, explicit capture, bank picker and macros remain available. Extra custom Grip bindings and advanced commands remain under More. G1 keeps its independent floating keyboard.
- Change key edits inline: action, bank/shared scope, old chord, physical capture, exact conflicts, Apply/Cancel. Work-object/orbit/catalogue interaction is disabled during capture but Current/Next/protection remain readable. Escape from every editor child cancels. Rejected input/page changes preserve the candidate. Closing releases held input and restores graph focus only if the application is still foreground.
- Direct step editing rejects busy/executed/capture states. Save compares original phase recipe and full state hash, preserving typed content rather than overwriting an intervening append.
- Untouched startup now focuses the existing Keyboard button before connection. Loading completion never steals focus from deliberate controls or another application.21 focused startup assertions passed before the dock work; full workflow verifies subsequent ownership.
- Retained Form loading enlarged the requested minimum. Set the experiment's1000x650 minimum in Shown, matching the existing NativeHost boundary. Do not disable autoscaling. Actual compact bounds now verified.
- Final image review found clipped inverse/chord text at984px. Final Keyboard source2769F1FC changes only action-row widths and `Click / inverse`; physical Shift inverse remains separately shown. The final223319 Native run verifies both sizes and complete inverse/macro text. Hub comparison painting was subsequently corrected within the same compact header;61 focused checks and the final Native render passed.

## Identified builds and evidence

- Final sample: `native-build/a156c3e260d97336`, executable SHA256 `704eb7ddf5717eb16f47ed28a7db0fe9001ac59b497c1ccadf400258f1e8c057`. Actually launched in the existing Instrument review session. UIA verifies the identified title,259800 slots/1200 generators, unchanged CurrentI35778/P2712 and NextI26789/P175618, and the embedded keyboard with20 Grip/seven Twist controls. The33-I bank intentionally remains uncaptured. Launch/evidence: `native-baseline/instrument-launch-a156c3e260d97336` (verification.json, initial-uia.json, keyboard-uia.json). The first immediate post-input UIA snapshots were temporarily null/stale; a supported window re-selection recovered the full tree. Process Responding remained true and no input guard was weakened.
- `native-baseline/workflow-20260915-220112`: exit0,413 assertions. Real WinForms/MPUlt with full model/transaction routes; explicit synthetic E1 two insertions, first-block protection, Next, cancellation, stale context, undo/redo, inline editing, banks,1/2/5/20-cell and filter cases. Actual1280x720 and1000x650 outer sizes recorded. Assertions include layout containment, not413 user scenarios.
- `native-baseline/workflow-20260915-220613`: exit0,88 assertions. Review/stage explicitly selected E1 inverse, then keyboard open/close, foreign bank/return, edit Cancel/Apply. Exact pending token/certificate/guard/executable status, full state, identities, draft/reference/block/protection preserved. Explicit final Cancel; no commit. No physical-key trial claim.
- The full413 and staged88 suites compiled identical22 application sources. Relative to final a156,20 are equal; only Keyboard row layout/wording and Hub comparison painting differ. All4 backend sources match. Final `workflow-20260915-223319` passed311 focused assertions and matches ALL22 application/four backend hashes. Actual1000x650/client984x611 and1280x720/client1264x681 were recorded. Explicit C555/T1 and inverse appended two legal Prepare words; untruncated complete-effect review proved zero net label changes without staging/commit. The previous221850 run passed311 before the final header paint correction.
- `native-baseline/workflow-20260915-215801`:5 window-size diagnostics passed. `214831` legitimately failed compact sizing; `214026` failed a hidden-versus-visible test selector after329 checks. Do not report those incomplete runs as green.
- Hub disabled text:51 focused checks plus inspected220112 image08. Final comparison header:61 focused checks in `native-baseline/hub/comparison-text-final`, including two native CheckBox clicks and unchanged snapshot. Exact-RGB pixel matching was rejected after actual/reference rasters showed identical glyph extents but different edge colors; row coverage, fit and actual phrase inspection replaced it. All failed probes remain recorded. Details: `docs/reviews/keyboard-dock-20260915.md`.
- Official infrastructure entry passed `work/team-evidence/20260915T124757Z-1adf4ad9/report.json` in the actual checkout. This proves team tooling only.

`Open-Native-G2-Instrument.cmd` resumes the existing isolated `sessions/g2-instrument-review`; never rerun preparation/reset merely to demonstrate the slice. Its prepared E1 CurrentI35778/NextI26789 is recorded in REVIEW_EXAMPLE.json. Old user sample557166 remains untouched. Owned dbce intermediate sample was closed normally. The final a156 sample remains open; old557166 user window remains untouched.

## Supporting design tools and computer access

- Figma existing file `PfDqOqBB3f3WNDxzVoZkCB`, node4:2, now contains an editable vector keyboard/inline-edit study, titled **Magic 600 Cell · Keyboard in the graphical hub**. Actual browser canvas was inspected. It is a study, not model-effect or Native evidence. Callable Figma MCP tools were absent; supported browser UI was used, no AI prompt sent.
- Existing Lucid diagram `eacbff8c-5739-46d4-8c8f-8af654ccda86` was opened. First-use role/frequency questionnaire still blocks editing. User question is pending; do not fabricate profile answers. Optional feedback popup was closed. Branch source ready in `docs/specs/keyboard-dock-lucid.mmd`; no diagram edit this cycle is claimed.
- Computer Use works again in this new execution turn. Previous Escape latch was not bypassed. `@oai/sky` window inventory/UIA and Tab/Return work; coordinate clicks lack geometry, screenshots fail SetIsBorderRequired/E_NOINTERFACE. Do not weaken input guards to accommodate unsupported injected scans.

## Evidence limits and next work

Host1280x720, graphicsDPI96. Actual1000x650 control rendering is verified; requested2560x1600 is still clamped1300x740. PNGs are GDI native-control renders, not desktop/DirectX pixel captures. No high-DPI/RTX4070, physical rollover or human trial evidence. Snapshot/GUI adoption timings commonly0.66–1.51s include fixture polling; input-to-GPU latency remains unmeasured.

Current keyboard iteration is complete and the final sample remains open. The final compact/wide GDI images were inspected, including the corrected full header label. Relevant reviewers have no unresolved disagreement within this scope. This is not completion of all P1/G1/G2 requirements or user approval.

Next significant product bottlenecks remain native ordered-frame Grip capture/rebind, graphical reference and complete work-sheet input binding, required residual/endgame workflows and interaction latency. The pure G1 helper's6 real-model tests/132 C7 comparisons/48 structural cases are not a native frame editor. Local/Global currently show Actual hosting-cell geometry with filter-aware cell fading, not per-cut-piece DirectX transparency or forecast-linked geometry. Four equal-summary orbit pairs still lack a verified graphical discriminator. Continue isolated implementation; formal integration/P2–P6 waits for BOTH explicit user approvals.
