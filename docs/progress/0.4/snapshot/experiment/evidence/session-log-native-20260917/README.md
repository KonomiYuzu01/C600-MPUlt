> Archived development document, published 2026-09-18. This is a stopped, unreleased 0.4 snapshot, not a new instruction to resume work. Historical status and failed results are retained; the current status is in the [progress index](../../../../README.md). Local paths are anonymized; raw evidence and runtime files are not bundled.

# Native Session log test handoff

`SessionLogNativeChecks.Run(shell, host, ready, check, image, output)` is compiled but not executed here. The root owns the native run and final source/model receipt. Start at actual Home with no pending preview in a disposable Session; `ready` must not acknowledge completion summaries automatically.

Coverage prepared: actual C600/MPUlt exports; explicit check/apply; existing-preview cancellation; changed file and changed current-state refusal; malformed file; valid 2,200,000-byte JSON check through the dedicated route; full 259,800-label equality; imported predecessor undo/redo; before-import branch and current-preferences recovery; Home import without a fabricated solve summary. All files are beneath the new run output directory. No personal files, clipboard, native file-picker automation, or physical typing are used.

`compile/result.json` binds the two current test helpers to an isolated x86 library compile against captured 020650 native sources. Exit 0, 4.803 seconds; only three existing unassigned InputState-field warnings. This establishes syntax/API compatibility, not current native behavior. New import methods are invoked by their agreed reflection seams; root must compile and run them with the current application.

The latency helper only received the bounded viewport-owner activation fix. No latency measurement, packaging, independent solver session, or recording starts in this correction cycle.
