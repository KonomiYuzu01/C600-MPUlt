> Archived development document, published 2026-09-18. This is a stopped, unreleased 0.4 snapshot, not a new instruction to resume work. Historical status and failed results are retained; the current status is in the [progress index](../../../../README.md). Local paths are anonymized; raw evidence and runtime files are not bundled.

# Explicit macro variants — completed bounded stage

2026-09-16. Stop work after this stage at the user's request; resume only after the next reminder. No timed restart was created. This completes the scoped connection in [the stage specification](../../docs/specs/macro-variants-stage-20260916.md), not full 0.4 or all S05/reference/endgame work.

## Result

Macro details now compares two explicitly chosen records and distinguishes full net equality, exact inverse action, equality on affected orbits and orbits where neither acts. Results carry exact model, identities, versions, original recipes and costs; they are not prefix-protection certificates.

Save inverse and Save with R create independent personal records with existing source provenance. R is the explicitly selected legal word in chronological R^-1 / Macro / R, not a certified spatial frame mapping. Source group is preserved; source purpose tags, notes and pins are not copied onto a different action. Saving leaves the selected macro, draft, targets, protection and preview untouched. Selecting the saved canonical entry is a separate action, followed by the existing explicit Add and transaction workflow. Existing records and exchange format remain compatible.

Comparison and saving expose Stop check. A shared input barrier waits for both the original result and the Stop acknowledgement, so a delayed Stop cannot be admitted alongside a new operation. An unknown/failed acknowledgement keeps execution unavailable; a successful saved receipt is never described as rolled back. New commands append to the shared Macro bank; existing keys retain their assignments. Help documents scope, operation and recovery.

## Fresh evidence

| Check | Actual outcome |
| --- | --- |
| New real-model variants | 12 tests passed; log (`real-model-tests.log`; local-only reference). Independent primitive replay, exact binding, scope, original recipe, source group, stale R/version/recipe, aggregate limits, cancellation, save failure, import/export/restart and misleading inherited metadata. |
| Existing library regression | 14 tests passed; log (`library-regression.log`; local-only reference). |
| Retained core/reference/crash | All three commands exited 0 via `work/team-evidence/20260915T234150Z-d23a36a8`; original generated reports restored byte-for-byte. Captured reports (`retained-checks.json`; local-only reference). |
| Retained native host/layout | Fresh WinForms self-test exited 0 via `work/team-evidence/20260915T234402Z-767a2aa1`. This is host/layout evidence, not GPU performance. |
| Native G2 | `postapproval-g2-20260916-085608`, exit 0, 339 assertions. |
| Native G1 | `postapproval-g1-20260916-085932`, exit 0, 168 assertions. |

Exact build identities (`native-verification.json`; local-only reference) bind all 45 input files for each native run; every hash matched the final files. G2 executable SHA256 `a5ade6a5da43e12e5a66c2102c33163570bd36da1f9dfdd0ce9f0a8b26755d71`; G1 `c7f01b46f61d75320085ca87ae5d2e592b6ec3ce9b77551f8505343ebfa8bc85`.

G2 actually chose the retained forward macro, compared its inverse, cancelled a proposed save, created and explicitly selected an inverse, compared it with the retained insertion, created an explicit R=[1] variant without changing the solving choice, restored R explicitly, and used the saved inverse through Add → full check → preview → fixed-sheet reuse with a fresh review → explicit execution. It then protected the first Home result, activated locked Next, executed the second supplied insertion and checked both block requirements, checkpoint, undo/redo and window reuse. It also compared and requested Stop while a separate staged preview existed; pending work and execution context survived.

Both native runs include a deterministic delayed-Stop admission fixture. It uses the real native input boundary and a test-only loopback acknowledgement; private busy true/false models the first request finishing before acknowledgement. A subsequent Send was rejected without work changes, then a newly issued command succeeded after acknowledgement. This is not a production-engine long-job race, physical-key trial or human solve. G1 separately rechecked real native Grip/Twist, exact Shift inverse, feedback, held input, focus and saved-key compatibility.

## Screens and corrections

Root inspected actual native controls and generated GDI control images at the same small-window state. Representative new-tool images in the final G2 directory are `14-macro-compare.png` (640×520), `15-macro-saved-inverse.png` (640×520), and `16-macro-r-variant.png` (716×519). Current, locked Next and protection remain visible. `desktop-help.png` and `desktop-keyboard.png` are FFmpeg desktop captures; the macro images are GDI control renders, not DirectX captures.

The first new native run `084855` stopped when the fixture posted another dialog while the trailing macro-effect inspection was busy. The application correctly refused it. The fixture now waits for readiness in the posted UI action; no implicit application queue was added. Independent review also corrected the placeholder assertion, source-tag/purpose inheritance, unreachable Stop controls and the delayed-Stop race. [Independent review](../../docs/reviews/MACRO_VARIANTS_REVIEW_20260916.md) records evidence boundaries and the resolution. No unresolved disagreement remains in this bounded scope.

## Remaining limits and next session

- No complete geometrical-reference certification, persistent verified relationship grouping, all-orbit residual/endgame acceptance or human completion evidence is claimed.
- The delayed-Stop success path is exercised natively; failed-acknowledgement lockout was reviewed in source but not fault-injected in the final native run. Recovery by reopening an isolated session remains the documented conservative path.
- Environment remains the observed 1280×720 logical / 1920×1080 captured Windows desktop with 150% virtualization and Intel HD620 evidence; no new 2560×1600, RTX4070 or physical input-to-GPU latency claim.
- Small-screen Keyboard/Macro Base obstruction, measured transport/adoption latency, Actual Puzzle detachment, 30 continuous cycles / 10 sheet reuses / all 35 moving orbits, consolidation, packaging and final continuous solver recording remain future work.
- Preserve approved G1/G2 state and personal sessions. No intermediate user test package, formal integration, public release, new schedule or completion-triggered backup was performed.

Use the leading [handoff](../../HANDOFF.md), the independently sourced [coverage checklist](../../docs/reviews/TOMORROW_COVERAGE_20260916.md), and the desktop project's `outputs/magic600-next-session/START_HERE.md` to resume. Do not turn missing evidence into assumed defects or count assertion totals as user scenarios.
