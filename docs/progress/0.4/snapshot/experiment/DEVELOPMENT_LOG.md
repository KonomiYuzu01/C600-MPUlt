> Archived development document, published 2026-09-18. This is a stopped, unreleased 0.4 snapshot, not a new instruction to resume work. Historical status and failed results are retained; the current status is in the [progress index](../../README.md). Local paths are anonymized; raw evidence and runtime files are not bundled.

# Magic 600 Cell G1/G2 experiment log

## 2026-09-17 — manual endgame and cross-orbit continuity

Extended existing Session/Solve/Macro Base with explicit known endgame families,
proper-frame reference variants, bounded current-use comparison, exact versus
position-only protection and journal-derived residual delta. Commit publishes
the complete state/context/protection transition atomically; restore never
reinstates old execution permission. No automatic parameter search or new
endgame workbench was added.

All35 explicitly constructed legal paths /59 stages pass with earlier orbits
protected. C2/C5/D5/A5 unfinished orientation save/reopen/undo/redo/restore retain
all labels and canonical identities. Native reproduced and fixed same-orbit
checkpoint restoring backend macro A while still showing B. Final focused native
002905 passed104 checks with unchanged123 bound inputs. Independent screenshot
review corrected missing scrolled parameter context, ambiguous reference-result
wording and an overlong tooltip. Repeated owned-editor focus checks passed; an
earlier isolated assertion failure remains unexplained and is not erased.

Full evidence, test-fixture failures and limitations:
`docs/reviews/endgame-integration-20260917.md`. Final continuous native acceptance,
packaging and the solver recording are still outstanding; no release occurred.

## 2026-09-16 — versioned residual and classification integration

The fixed transported chart now drives classification and exact UseScore; full action
and unknown references remain explicit. ReviewContext ties the operation, role/reference,
Next, model/frame/naming and protection evidence together. Independent review reproduced
two cache defects: incomplete frame evidence could not recover, and cancellation could
bypass cached-analysis publication. Seven targeted regressions failed before the fixes
and passed afterward (`evidence/effect-cache-boundaries-{red,green}.log`). Updated legacy
contract expectations preserve explicit keyboard-bank switching and per-orbit drafts:
35 pass, one optional engine smoke skipped (`evidence/review-contract-regression-green.log`).

Native203841 and its focused204321 replay failed the Solve foreground assertion; no
OS/app cause has been proved. Added focus-owner evidence. Native205254 then passed32
focused macro-use assertions with actual desktop capture and unchanged bound inputs.
This is not a focus bug-fix claim or complete native acceptance. The desktop still shows
Solve/Keyboard obstructing the hub; that remains in the shared steering queue.

ResidualState helper and policy/context binding now pass12 checks; adapter integration
passes8 new and14 affected checks. Analysis is explicit/cancellable; passive snapshots
read only matching results. A newly staged recipe cannot be mistaken for retained
executed steps. Independent review identified the replace-pending rebind edge, currently
being corrected before graph integration. No new endgame workbench or parallel state.
The invariant component separately passed all1200 legal generators across35 orbits,
with9 tests and a bound receipt at `evidence/orbit-invariants-20260916-validation.json`.
These are necessary-constraint checks, not executable-solution or full-solve evidence.

## 2026-09-16 — exact names, operation cycles and orientation foundation (active)

The latest integration/endgame contract is captured in architecture addendum08.
The user's u0/u1 decision resolves the six anchored-reference address splits without
inventing new orbits or chirality. Exact structural names and typed canonical parsing
now cover the433 reference stickers and177120 fixed positions. Eighteen naming and
six isolated input checks passed; native195547 passed Piece/Position copy, wrong-kind
rejection without lost input, apply and cancel. Earlier native clipboard failures remain
recorded; detached fixture backup/restoration resolved that replay, not a proven
application or OS root cause.

Native operation cycles now replace the obsolete coarse triangle projection. Complete
support is computed before paging. Actual occupants and after-Prepare occupants are
distinct. Fixed empty-cycle negative indexing, first-selection loss and cursor/context
defects. Focused193609 passed29 assertions with actual desktop evidence; a preceding
ring-focus failure is not erased. Full updated regression195900 is in progress.
Functions single-key entry/return and visible modifier chords were verified through the
earlier partial full run; full completion is not yet claimed. Sequence hover text was
shortened after observing obstruction, preserving complete inspect/edit information.

Ten orientation-helper tests passed against all1200 legal generators and full259800
slot reconstruction, including D5/A5 noncommutative composition. This is a read-only,
versioned chart, not new Session state. Independent implementation review remains;
old classification/scoring and new endgame intents are not yet integrated. Audit found
real historical endgame witnesses and fresh full-action replay equality, including the
98372-primitive A5 final-buffer recipe and its O33 collateral. Numeric limits allow it;
native response, strict-prefix safety and all-orbit operational coverage are separate work.

Final recording must follow one compact solver path, beginning with improved G1/G2,
then composition/protection/cross-orbit/endgame. Mark skipped or accelerated waiting,
keep actual action/result correspondence, and place concise subtitles away from
essential geometry/input/protection. Internal verification remains broader than the film.

## 2026-09-16 — explicit macro variants connected through native execution; day stopped

Finished the bounded macro-variant stage, retaining original records, exact IDs/recipes and the Session transaction boundary. Native Details has explicit pair comparison; new inverse and selected finite-R variants save independent entries, followed by separate selection and Add. Source-group and exact-source/R guards prevent unrelated-work classification or stale creation; aggregate limits, cancellation and failed storage preserve work. New metadata does not copy an old action's purpose/direction/pin. No automatic solving/search or persisted-format migration.

Independent reviews found and corrected inaccessible Stop controls and a delayed stop-job race. A global acknowledgement barrier now blocks the next command until the original request and Stop settle; unknown acknowledgement locks execution without falsely rolling back a saved result. Controlled native delayed-ack checks pass; error-latch source evidence is distinguished from native fault injection.

G2 085608: exit0/339 assertions, including created inverse → exact comparison → explicit selection/Add → full review/preview → worksheet reuse/recheck → execution → protected first result → Next/second insertion → undo/redo. G1 085932: exit0/168 with compact Keyboard, exact Shift inverse, input feedback and compatibility. Both final manifests45files match current source.12 new real-model and14 existing library tests pass; fresh core/reference/crash and native host/layout pass. The earlier084855 failure was a fixture command posted during a legitimate macro-effect busy interval; fixed fixture readiness, no application queue introduced. See `evidence/macro-variants-20260916/REVIEW.md`.

Updated explicit next-session checklist and shared memory; stopped per user. Full0.4, spatial-reference certification, residual/endgame coverage, sustained-work acceptance and final recording remain incomplete. No new schedule, intermediate delivery or public release.


## 2026-09-16 — compact Keyboard and completed native follow-up

User requires a compact final onscreen Keyboard. Same-state native comparison reduced the outer window1056×540→880×490 without reducing the body font: five visible main rows, explicit modifier-key expansion, direct unmodified custom-key access, concise shared identity/protection readout and full focused cap/frame meaning. Old Backquote Grip/Twist bindings are preserved; the new disclosure command is editable. Independent review corrected stale focused-key descriptions, generic frame naming, Shift display/dispatch mismatch and reset/lost-key-up false reverse feedback.

G2 `081709` exited0 with307 assertions, completing E1 insertion/block/Next/worksheet/Macro/Help/layout/protection workflows previously blocked by exact scroll and wrapped-readout defects. G1 `082150` exited0 with162 assertions, using the final current source and covering visible press/release/latch/rejection, legacy Backquote and Numpad remapping, focus/release safety, actual Shift inverse key routing and full259800-label preservation. G2 predates only the final Input Shift-release correction and G1 fixture extension; final G1 and19 direct-router tests cover them. All final G1 build-manifest source hashes match the working files.29 offscreen caption/layout and9 exact-model frame tests also pass. See `evidence/compact-keyboard-20260916/REVIEW.md` and `verification.json` for scope, executable identities and remaining limits.

Static comparison and actual desktop captures were inspected. Actual small-screen coexistence of Keyboard and Macro Base still obscures the central graph, even though persistent context remains readable; this is the next layout concern, not a reason to shrink text. Overall0.4/continuous solver recording remains incomplete. No intermediate test build was delivered and no public release was made.

## 2026-09-16 — filtered Grip feedback and exact native follow-up (in progress)

Grip display uses authoritative affecting-cap membership intersected with an evaluated Piece Filter in Local and the committed Hub objects. Hold release, latched/pointer state and physical key depression are distinct. No filter changes a legal rotation or full protection. Local real-model tests:9 passed. The I108 witness has two hosting cells but18 affecting caps; C6 affects it without hosting it. Under piece(109), a different C1 region still belongs to C6, so the native negative fixture was corrected to exclude108 while expecting109 rather than falsely requiring no highlights. Actual Puzzle/Global do not yet have this extra visual overlay; their preserved filter and mechanical behavior is not evidence of new Grip-cue coverage.

The layout-only persistence path passed9 real HTTP checks and actual native timer/foreground-command checks in073118/073930: latest bounds persist without reserving the input gate or changing259800 labels. Required core/reference/crash wrapper20260915T223149Z-ba82286e passed all three commands, including1200 reference mappings; original source report files were restored byte-for-byte. These are not human solve or hardware power-loss evidence.

Independent review corrected two real scope/layout gaps: complete-operation effect counts now open complete-operation findings and do not display macro-body applicability; shared Current/Next/protection rows measure wrapped text in both floating tools and modal Help/Macro dialogs. Ten-topic detailed Help, rounded paint and12+19+7 synthetic input checks are integrated. Combined native073118 and073930 both fail the final keyboard row at maximum scroll; mathematical sum sizing alone did not resolve the actual timing behavior. These are failures, not passes. Native diagnostics and a new replay remain required; other Grip/Help checks will record independent outcomes even if keyboard exposure fails.

## 2026-09-16 — compact surfaces, concise copy and input feedback (in progress)

The user approved the small rounded visual direction after requesting an isolated display test, and clarified that detailed feature introductions belong in Help. Main surfaces retain objects, exact state, actions and concrete rejection reasons. Healthy boilerplate in Macro tools and worksheets collapses; Local keeps its actual/filter status and compact line-style legend. Detailed Help is a searchable owned dialog, using the existing focus boundary. New copy and Help still require a combined native run.

`rounded-surface-study-20260916-070114` exited0 on the actual retained native E1 scene. Two appearance changes preserved105 control bounds,101 complete button Regions and mechanical/work context. The interactive run did not execute its auto-only handler test. GDI images compare the same bounds/state; an actual1920×1080 desktop capture exposed a regression in forced outer Form.Region rounding: Windows switched to a legacy caption. That outer-frame technique is rejected; only paint-level button rounding is being carried forward. Native screenshot API returned E_NOINTERFACE twice, and the existing FFmpeg desktop capture worked. No other scaling/GPU/human evidence is claimed.

Compact native `065452` passed keyboard scroll reachability but failed operation-new immediately after bank. Logging/source tracing identified automatic window layout persistence competing for the foreground command boundary. A strictly layout-only authenticated acknowledgment route, under the same Session lock and without snapshot adoption or job reservation, is being integrated. Independent review also caught loss of hidden-window bounds during partial saves; those records now need merge preservation. Full combined native timer/command tests remain pending. Earlier `064422` failed worksheet protection text height after103 assertions; that is not a pass.

## 2026-09-16 — classification, placement and stricter native review

Native `061430` passed the existing 69 assertions with normal exit. Actual images showed worksheet details squeezing the Current/target comparison; expansion now grows within the work area, keeps Next/protection visible, selects the first nonempty phase, preserves the user's detail tab, and surfaces changed/missing revisions. Fresh exposure checks are pending. Read-only cross-review reproduced unrelated-monitor context shrinking a dialog; context reservation now considers only the target work area. First placement also avoids existing title strips when space permits. One hundred pure rectangle assertions passed; large-monitor geometry is not a native high-DPI test.

Macro Base now has detached validated metadata/facets/exchange helpers and atomic adapter integration. Independent14 helper and12 real-model integration checks passed, alongside classification5, worksheet5 and boundary9 regressions. Exact model/recipe/proof-version summaries outlive the40-entry permutation cache, bounded to256 summaries and4,000,000 serialized bytes. Imported claims are never proofs. Native metadata/filter/import/export controls retain canonical selection and single-key routes. Cross-review fixed dirty metadata lost on file navigation, an overbroad orientation label, and Unicode tag disagreement by using backend-derived tag keys. New Unicode integration check passed; native workflow remains under test.

The stronger native key visibility test FAILED in `063741`, reproduced with dimensions and a PNG in `064010`: the final supplementary row was2px clipped even at maximum scroll. Declaring the actual arranged content extent replaces the undercount; `064422` is the pending fresh run. Earlier69 passes did not cover this condition. No failing run is recorded as accepted.

BrowserAct1.4.2 now runs from the shorter isolated project cache after an identical-package/module comparison isolated a path-sensitive DLL import failure. Full workflow documents and empty browser list were read successfully; no browser/account operation was performed. Figma compact/wide placement studies were actually created and inspected; they remain design evidence. The user's latest compact-UI request is being applied without shrinking key targets or hiding capabilities. Actual Puzzle detachment is a separately reviewed, single-device native adaptation candidate; current code is not yet promoted or verified.

## 2026-09-16 — cooperative-window correction and reusable-method cycle

Fresh native `postapproval-g2-20260916-054201` passed 60 assertions and normal process exit: two explicit E1 insertions, protected first requirement, locked Next, detached Operation single-key execution, unchanged Local center, all five user-close/reopen paths and undo/redo. Independent G1 `postapproval-g1-20260916-054508` passed 15 assertions and normal exit. Both default command registries have complete single-key set coverage. Five isolated input-router checks passed. These are agent-operated native checks, not human solving evidence. Inspected GDI images confirm readable Current/Next/protection and bounded transparent excluded geometry; F1 clipping was then corrected to “Index”.

Macro review reproduced a false orbit-local-three-cycle label for legal word [2,1200] (2,060 cycles, no single-three-cycle orbit), and missing A/B/occupant-direction applicability reasons. Root corrected exact scoped classification and body applicability; five independent tests replayed all labels, including an orientation-only case, and passed. Body applicability never certifies the complete operation.

Reusable worksheets now preserve exact macro ID/revision/range/recipe provenance and compare saved versus current inputs, including inactive-orbit position locks. Explicit loading is bound to an ephemeral receipt containing exact sheet content plus live guard; same-name sheet replacement invalidates old confirmation. Legacy fixed recipes remain supported without invented provenance. Five real-model integration checks and twelve pure helper checks passed; native comparison/cancel/load workflow is under test in `postapproval-g2-20260916-055511`.

Measured repeated frame-table decompression dominated Local preparation and first-bank switching (diagnostic evidence `evidence/perf-postapproval.json`). Reuse only immutable rotation tables per Model instance and detached cap catalogues; labels/filter/protection remain live. The sole retained-source change is the backwards-compatible optional `rotations` argument in `grips.py`; assets, Session and production launchers are unchanged. Fresh exact frame/local tests and after measurements are pending. Current build manifests now include this modified shared source.

Next highest-impact work: complete native worksheet verification, resolve any lifecycle/focus errors, then finish exact Macro Base workflow and remaining continuous-work acceptance. No final 0.4 completion or new public release is claimed.

## 2026-09-16 — approved continuation, first cooperative-window cycle

User explicitly approved both G1 and G2 against the a156 native sample/recording. Remaining development is authorized; no intermediate test package is requested. Final delivery is a continuous solver recording, with all internal correctness and native checks retained. New contract is in docs/specs/post-approval-workspace-20260916.md and the architecture execution addendum.

Independent bounded owners implemented original 433-region Local meshes, exact cap-frame corner notation, mathematical naming (35 orbit classes, 600 golden-coordinate cells, Home/current distinction), and cooperative native Local/Global/Keyboard/Operation/Macro Base windows. Root integrated exact frame dispatch, explicit-bank-only transitions, goal-specific review, transported block requirements and compatibility defaults. No model asset, personal session or production launcher changed.

First native run `native-baseline/postapproval-20260916-051840` completed 53 control/state assertions, including two explicit E1 insertions, first-block protection, Next activation, zero-net forward/inverse input, detached single-key execution, Local center persistence and undo/redo. Six actual GDI control images were produced. It then FAILED normal shutdown: owned tool close cancellation kept the process open. After two normal close attempts root terminated only the identified fixture process. Its internal report exit_code=0 does not make the process run successful; actual process exit was1. Fix and fresh lifecycle run required.

Independent image review confirmed three high-impact defects: truncated key actions, auxiliary footer omitting Next/protection, and accumulated translucent triangles overwhelming included Local regions. These are assigned for correction, with no claim of accepted visual quality. GDI images are not DirectX desktop captures or human trials.

Fresh headless evidence: nine post-approval boundary tests passed (13.772s), nine native HTTP response tests passed (24.435s), six frame helper tests passed (16.419s), nine naming tests passed (4.138s), six original-geometry tests passed plus affected Next tests. Cross-review caught and corrected wrong-bank frame capture, shared-set turns, mixed-settings partial mutation, saved-context loss on Next activation, old-checkpoint missing defaults and captured orientation being overridden by Home.

Agents API continuation initially failed with confirmed credit_balance_exhausted. After user replenished credits and explicitly requested retry, the existing Agent/Session continuation completed without error; no duplicate resources were created. API analysis remains advisory and has no local execution environment. Apply returned findings through local evidence. Release notes must cover actual changes only; research updates follow formal release and must not claim a human complete solve.

## 2026-09-15 — Utilities, Solve and keyboard form one work context

Latest user clarification: graphical Utilities exist to make the actual Solve mechanisms convenient from the keyboard; design the mechanism and command routes together, rearranging the experimental UI when justified. Avoid AI-style recommendation/automation and unfamiliar interface jargon. Preserve exact mathematical terms where necessary and explain them near the relevant object.

The observed limiting factors were lost preparation and incomplete protection visibility. Independent source reviewers found native macro insertion omitted `append`, replacing an existing phase; phase buttons edited JSON but did not select the input destination. The shell now appends the explicitly chosen fixed macro, registers phase/destination choices, consumes bank macro records as explicit selections, shows effective shortcuts and exposes complete review results. No automatic macro choice, setup or target solving was introduced.

Measured baseline `native-baseline/workflow-20260915-061528`:29 checks passed with real WinForms/MPUlt and full-model HTTP transactions. The content measured1616px wide in a1264x681 client; graph height361px. Changing AutoScaleDimensions did not fix this and was removed. A percent-width root, bounded control rows and a bottom Macro Base band now retain a full-width graph. `workflow-20260915-063010`:36 checks passed; open-catalogue graph1264x410, rows31/29/410/124/33/31/23. All four E1 positions and protection lines are visible in actual GDI control renders. This is not desktop/DirectX pixel capture or a human trial; actual screen1280x720, graphics DPI96, Windows scaling percentage not established. Live canonical600-cell palette matched the graphical palette exactly.

Post-pass independent review found modifier-alias disagreement, bank-slot focus loss, stale keyboard-window bank text, modal chooser focus override and a missing out-of-work conflict object. Corrections are being verified against a fresh final build. A separate real legal O32 witness demonstrated Home-position/wrong-orientation current capture failure; explicit `capture_current` now preserves its actual labels without changing the existing Home requirement or persisted member schema. The focused test failed before and passed after the correction, preserving labels/head/Next/protection. Five cancellation input checks passed (synthetic routing); actual native job cancellation remains separate evidence.

G1/G2 remain independently Pending. Ordered-frame G1 capture, complete native graphical reference binding, full work-sheet inputs and remaining acceptance scope are not complete. Current work is isolated P1 and must not be promoted as an accepted0.4 build.

## 2026-09-15 — Graphical native workbench, color and continuous-work defects

The limiting factor is accurate repeated work on graphical objects, rather than additional panels. The user explicitly requests color in the graphical UI. Use original canonical cell colors for structure, separate restrained state colors with textual/line distinctions, and no invented orbit-color semantics. The root owns Shell/Bridge/adapter; the interaction reviewer owns Hub; the keyboard reviewer owns the input router and its fixtures; the solver reviewer owns focused backend regression additions. No production integration is authorized.

Observed native baseline: `native-baseline/workflow-20260915-053231` executed the explicit E1 two-insertion sequence using real WinForms buttons, retained MPUlt rendering and the authoritative HTTP transaction boundary. Twenty-one checks passed, including macro inspection/use, review/stage/cancel, both insertions and graphical protection of X. The final check failed because the test incorrectly expected Next after explicit activation. S02.3 requires clearing that slot; only the test was corrected. This full run remains failed until rerun. Form.DrawToBitmap images were blank and are not accepted visual evidence; the next capture targets the actual content control. Sky's native screenshot path fails with `SetIsBorderRequired: E_NOINTERFACE`; accessible state and native interactions are separate evidence.

The reviewers independently reproduced three native-control defects: stale local selection after authoritative Next activation, first-open context menus closing on asynchronous inspection acknowledgement, and absent Current/Next outside the displayed roles/block. Corrections and the colorful graphical pass are in progress. A separate real-model cross-orbit Next regression failed at `33-I != 34-I`, then passed after matching the activated orbit's insertion bank; no labels/head, draft or activation semantics changed. A focused prediction regression passed against primitive replay: current complete Ready/Staged review only, exact Current/Next/buffer/block occupants, no full render payload, and no prediction when stale.

The shared split factory passes eleven WinForms checks, including reproducing the user's original min-before-Size exception and rejecting the old470px nested child overflow. Optional redacted input diagnostics passed three focused checks; actual F1-on-dropdown behavior remains to reproduce with that instrumentation. G1 frame audit established132 C7 full-label conjugacy comparisons for12 explicit frames and11 rotations; implementation still lacks ordered-frame capture, so this is not G1 acceptance. All reports remain agent evidence. G1 and G2 user decisions are separately Pending.

## 2026-09-15 — Internal workflow review and native approval carrier

The latest user instruction requires independently runnable G1 and G2 samples in the retained Windows WinForms/MPUlt/DirectX program. Browser experiments are internal evidence only. Both user approvals remain Pending. Ordinary isolated development and verification are authorized; architectural direction changes still require confirmation. These rules supersede the historical pause below.

Internal changes: a coherent work scene replaces duplicated Block/Operation views; Current/Next and exact position sockets remain distinct; strict typed identity inputs, fixed-position inspection and centralized display conversions replace ambiguous numeric routing; role, macro, bank, filter and worksheet work retains explicit human control. Backend corrections preserve pending previews on rejection, validate real inverse actions, reject capture gaps and mark staged versus stale reviews accurately. ID_AUDIT.md records authoritative mappings. Piece IDs remain visible by the latest user instruction. Orbit descriptions use verified model structure; four structurally similar pairs have no claimed handedness classification.

Evidence is bounded by build/file hashes in each report. Latest internal checks: 21 experimental model/engine tests, 14 keyboard router tests, 8 ID tests and 25 graphical semantics cases passed. Full core/reference/crash checks passed. The lifecycle suite reached 20 passing checks then failed a pre-existing literal source assertion expecting `native/snapshot` while retained code uses protocol 2; the entire suite is not passed. Agent-operated Edge workflows include the legal synthetic two-insertion E1 witness, hidden protection, draft/context continuity and 1280/2560 captures. These are neither human trials nor native DirectX evidence. Remaining browser observations include 10 px G1 cap labels and a 2.662 px focused-socket edge clipping at 528 px; browser polish is paused while native samples take priority.

Native preflight found the retained x86 Framework compiler, all approved Managed DirectX assemblies and the expected MPUlt hash/reflection surface locally. Compilation succeeded. The first unchanged WinForms fixture timed out after 120 seconds in native-baseline; investigation is active. Computer-use list_windows and list_apps timed out separately; no permission refusal has been observed. Do not infer GPU or native rendering failure from these errors.

Ownership for this cycle: root owns native shell/renderer transport and adapter; keyboard reviewer owns isolated ExperimentInput.cs and its regression; interaction reviewer owns ExperimentHub.cs and focused graphics checks; solver reviewer diagnoses the native fixture in a separate directory. No production code, model assets, production launchers or personal sessions are changed. The decisive next checks are fixture completion, atomic native/work-context transfer, actual native startup, and explicit same-session operations through the existing transaction boundary.

## 2026-09-15 — P0 preflight and interrupted initial scaffold

Baseline: source HEAD `5e1d35de792ccc8db896b8abf74ff2b1b00e0749`. Existing changes to SOURCE_MANIFEST.json and three development documents were preserved. Immutable assets, production launcher and personal sessions were not modified.

Created experimental `engine.py`, `launch.py` and `adapter.py`. These are incomplete and untested. No frontend has been created and neither G1 nor G2 has been launched. The proposed runtime uses an AST-installed route hook in the existing server process; it is not a validated extension contract.

Three independent reviewers completed bounded preflight inspections. Solver review identified automatic setup fields and incomplete new-policy commit guards. Keyboard review identified outer-axis ordering and nested-inverse-label hazards; it reported full-model inverse/order calculations for four explicit cap fixtures before the latest pause instruction. Interaction review identified browser/native picking differences, stale draw-list risk, reference click-through risk and missing renderer disposal behavior. No reviewer operated a runnable experimental UI; none of this is human-trial or native-UI evidence.

The user's latest explicit instruction requires confirmation before resolving technical uncertainty or running an unapproved validation experiment. All affected implementation and experiments are now paused. Read-only investigation and administrative recording remain permitted. Reviewers have been notified.

Pending decisions:

1. Native WinForms/MPUlt experiment container versus an isolated browser shell reusing the existing WebGL renderer. Browser behavior is not proven equivalent to native picking, input or lifecycle contracts.
2. In-memory AST hook versus an explicit extension in an isolated source copy. Startup, compatibility and lifecycle of the drafted hook have not been tested.

Any proposed minimal validation must receive explicit user approval before execution. Retain the current scaffold as unvalidated work; do not silently treat it as the selected implementation. G1 and G2 approval statuses are both Pending, and production integration remains prohibited.

## 2026-09-15 — Full frontend rejection and redesign, interrupted

User rejected the previous native UI. A stable object-centered block map was chosen over a standalone Before/After list because repeated solving needs stable block context. New Shell/Workspace/Tools integration is unfinished and unverified; no new runnable build delivered. Native Computer Use reported physical Escape, so app input stopped and all reviewers were interrupted. Current source ownership and resume steps are in HANDOFF.md.

Independent authority regression: three prior Ready/Staged/template revival counterexamples reproduced; seven focused checks pass after the existing ephemeral epoch invalidates successful relevant intent/protection changes and explicit template reuse. No persisted schema/model identity changes. Native integration and independent review remain pending. Macro-effect acknowledgements without insert-macro, followed by empty-draft rejection, establish a missing visible workflow transition. Latency diagnosis remains incomplete; no performance improvement claimed.

## 2026-09-15 — Refined native sample, verified fixes, user requests deeper redesign

Final build557166c3dc1f2353 completed native-baseline/workflow-20260915-082223 with121 assertions, real WinForms/MPUlt plus full-model transactions. This is an explicitly synthetic E1 workflow, not a human solve or121 user scenarios. NATIVE_REVIEW_GUIDE.md and Open-Native-G2-Refinement.cmd provide reproducible independent entry. The sample was launched in a fresh isolated g2-graphical-refinement session and verified ready through native diagnostics and actual UIA.

Observed limiting failures during this cycle were lost/ambiguous macro insertion, stale review revival, cropped controls, invisible keyboard mapping lines and modal focus loss. Changes preserve Add/Replace distinctions, keep fixed macro effects separate from complete current reviews, invalidate stale authority through ephemeral epoch, fix nested layout scaling, render readable two-line Grip/command buttons, keep Grip and seven Twist controls visible at compact size, and return focus to the actual owning keyboard control. Finite steps preserve unadded text and drafts. Pointer forward/inverse preparation was compared over all non-truncated affected label pairs with the original fixed macro; explicit cancellation preserved exact action.

Independent reviewers reported mathematical/graphical protection defects, keyboard focus/mapping defects and native transport duplication separately. Root integrated verified corrections. Final independent GDI image review confirms cap/key second lines and compact Twist visibility; Pointer inverse still needs scrolling in compact view. That remaining defect was recorded, not changed after the user stop. Agent review is not human approval.

Additional evidence:7 focused epoch regressions and5 real-model HTTP transport checks passed;19 synthetic input/WinForms-message checks passed. NativeHostRegression19 passed with renderer stub, explicitly separate. Payload size fell3684780 to117917 bytes; some route timings improved but empty-draft latency varied/regressed and GUI adoption remains hundreds of milliseconds. No global performance claim.

All exported images are GDI control renders. Host1280x720, graphicsDPI96; requested2560x1600 was clamped to1300x740. No high-DPI/RTX4070 validation. Sky screenshot failed SetIsBorderRequired/E_NOINTERFACE. Final actual sample click lacked coordinate geometry and tool-delivered F1 was rejected as unsupported scan; the accessible status and redacted diagnostics establish this limitation, not physical hardware behavior.

The user then explicitly stated the current abstract sample still contains many earlier problems and requires redesign from underlying solving and interaction logic. They asked to stop today and resume tomorrow. Development, testing and app input stopped. Memory/handoff updated and one-shot task continuation set for2026-09-16 10:00 Asia/Tokyo. The current frontend is not accepted. G1 ordered frames and G2 graphical references/work-sheet inputs/endgame remain unfinished; both approvals Pending. Next cycle must address the central workflow instead of continuing surface refinements, preserving useful verified foundations.
Schedule correction: the user explicitly requested 2026-09-15 at 10:00 UK time. Windows GMT Standard Time resolves this to 09:00 UTC / 18:00 Tokyo. Updated the existing one-shot heartbeat resume-magic-600-cell-redesign; did not create another task or resume development.

## 2026-09-15 — Shared structural scene and continuous manual operation

The user explicitly resumed development and required a substantial redesign of the core graphic as a precise spatial instrument. The old scheduled continuation was paused to avoid a duplicate run. This cycle stayed in P1, retained the existing WinForms/MPUlt renderer and Session, and did not alter immutable assets, model identity, personal solves or production launchers. G1 and G2 remain separate Pending decisions.

The principal change is one common projection of block/Target hosting cells instead of independent thumbnails. A/B preparation regions, fixed P sockets, solid actual I tokens, hollow finite-phase forecasts, certified macro cycles and protection now have distinct meanings. Current/Next and the complete protection boundary persist while catalogue/operation tools open. Operation editing is on demand; macro-body and complete-operation inspection are explicit separate display scopes. Real cell colors are retained. Shared geometry communicates incidence, not rigidity, orientation proof or setup.

Underlying workflow corrections matter as much as layout: keyboard banks no longer select a work orbit; inactive-context position protection remains effective; exact Prepare/Macro/Cleanup inspection is paired atomically with work state; retained executed steps require explicit New or Reuse. Durable commit/workspace-save exceptions are reconciled only against exact recorded evidence. Repeated keyboard controls are reused, and every acknowledged bank/context change clears held input immediately. A reproduced modal-owner return error was fixed by assigning the active owner before ShowDialog; the native regression now checks real modal visibility and the close-time error, rather than accepting a visible dialog as proof of a successful cycle.

Independent work used solver, keyboard and graphical reviewers with disjoint file ownership. Source-derived suspicions were tested rather than voted on. The invalid protection-enabled payload allegation was withdrawn because existing entry validation already rejects it. The20-cell screenshot initially suggested unbound macro roles; exact export proved all three roles match and the displayed extra cycle is collateral. The correction therefore clarifies principal/collateral cycle scope and keyboard location, not invented A/B/Target assignments. Actual Local/Global now explicitly identify committed geometry when the main hub shows a forecast.

Fresh whole-native run workflow-20260915-194904 passed267 assertions, including containment checks rather than267 independent user scenarios. It used real WinForms/MPUlt, complete model data and authoritative transactions: two explicit E1 insertions, first-position preservation, Next activation, cancellation, hidden conflict, stale review, macro Add/Replace, bank/focus/editing, New/Reuse, undo/redo and1/2/5/20-cell contexts. Its diagnostics contain only the deliberately rejected protection/executed-step cases, not the previous circular-owner exception. Two final cycle/provenance presentation corrections require the subsequent identified native run recorded in HANDOFF.md.

Focused evidence: operation lifecycle20, finite phase10, position protection11 and native transport9 tests passed against final backend sources; Hub explicit-scope51 checks passed before the final cycle refinement. Input adoption7 passed after6 baseline failures; existing input19 plus focus4 passed. Keyboard reuse14 checks passed. The separate mathematical Grip-frame helper passed6 real-model tests, including132 C7 comparisons and48 representative structural cases, but is not integrated into a native ordered-frame editor. Team infrastructure verification20260915T105852Z-7208e411 passed; this certifies team tooling only.

Images are GDI control renders, not desktop or DirectX pixel captures. Actual screen1280x720, graphicsDPI96; requested2560x1600 Form was clamped to1300x740. No physical high-DPI/RTX4070 or human-solving claim. Command-to-adoption latency remains commonly about0.6–1.2s including fixture polling, sometimes higher. The control-only keyboard median398.7ms→30.2ms does not establish end-to-end latency. Complete graphical reference editing, worksheet inputs, residual/endgame coverage and ordered-frame G1 capture remain unfinished. Detailed decisions, withdrawn findings and evidence limits: docs/reviews/instrument-20260915.md. Consolidation scope: docs/CONSOLIDATION.md.

## 2026-09-15 — Piece Filter and identified Native interaction slice

Final sample8bbde56fee511a1c, executable SHA25621cf1390ceb4f356c2d6cb2af71e1537f4c6fd003623ed05355574fb8be80f77. Final native workflow203951 passed288 assertions, exit0; all22 application C# and4 recorded backend hashes match the sample. Actual launch/UIA verified all259800 native slots and1200 generators, prepared E1 CurrentI35778 and locked NextI26789. The sample remains isolated and open. No user approval or production integration.

Piece Filter now reuses installed authoritative Session rules for Actual and each finite boundary. Identity filters follow physical pieces; position filters retain fixed positions. Unknown results are distinct from excluded tokens. The hub dims excluded tokens while retaining IDs, Next and protection; Local/Global fade cell context with zero matching stickers and preserve manual selection/origin. Their cell geometry is not an individual cut-piece mesh and the retained Puzzle ghost is not new DirectX alpha transparency. New F5 presets require explicit Apply. Compound edits use installed rules rather than stale workspace text, and new piece/position arguments receive range validation without rewriting stored data. Twelve model tests, eleven tool checks and sixteen Hub checks passed; native images17/18 confirm readable preset IDs and Actual Local mathematical captions. Empty filter has zero interactive native stickers; a20-cell identity has exactly20.

The final actual-sample review found startup focus in the orbit dropdown, leaving F5/F9 under text-input suppression. This remains uncorrected, despite the broader workflow passing. Candidate: one initial guarded Hub focus handoff, never recurring adoption or main-window activation; preserve deliberate startup interactions. Independent keyboard source review highlights that opening then closing the orbit picker during initialization needs an explicit discriminator if retaining that deliberate focus. No correction or test was started for this new issue.

Computer Use returned physical Escape. User explicitly identified accidental Escape and authorized recovery. Normal retry still returned the stop state. The supported JavaScript reset and package initialization succeeded; readonly list_windows still returned the same per-turn stop. No further UI input, custom helper, alternate automation, permission change or security bypass was used. Final capture also returned SetIsBorderRequired/E_NOINTERFACE. The next minimal recovery step is a new execution turn, starting with readonly tool verification; no guarantee that this clears the host latch has been claimed. Current detailed handoff and shared memory are updated. G1/G2 remain independently Pending.


## 2026-09-15 — Keyboard settings integrated into the graphical hub

Delivered isolated iteration a156c3e260d97336; executable SHA256704eb7ddf5717eb16f47ed28a7db0fe9001ac59b497c1ccadf400258f1e8c057. It was actually launched and resumes the existing Instrument session. G1 and G2 remain separate Pending decisions; native ordered-frame G1 and complete G2 reference/work-sheet/endgame scope are still unfinished.

The limiting interaction was leaving the main object context to operate or configure the keyboard, compounded by focus ownership and compact-layout defects. G2 now uses one bottom work region for Keyboard or Operation, with20 Grip/seven Twist controls, current bank/cap/frame/destination, explicit capture and fixed macro slots. Command keys edit inline with exact conflicts, scope, Apply/Cancel and keyboard capture. During editing, work-object/orbit/catalogue interaction is locked while the graph, Current/Next and protection stay readable. Held keys are cleared. Closing only restores foreground application focus. G1 retains its independent harness.

Independent reviewers exposed a busy direct-step editor overwrite path, local selection before capture rejection, incomplete Escape handling, disabled-text contrast and clipped compact inverse/chords. Corrections preserve typed candidates, original draft/state guards and all Session semantics. Actual compact-size assertions also exposed retained font autoscaling enlarging MinimumSize; restoring the intended minimum at Shown, as in NativeHost, fixes real1000x650 behavior without disabling autoscaling. The final standard comparison CheckBox needed compact text painting; its native Checked/events and preview semantics were retained. A faulty exact-RGB raster oracle was replaced only after saved rasters demonstrated equal glyph extents but different edge colors.

Verification: full Native220112413 assertions (two explicit E1 insertions, first-block preservation, Next, cancel/stale, undo/redo, filtering and input); staged-keyboard22061388 assertions preserving the exact preview token/certificate/guard and all work context through bank and key edits; final223319311 compact/wide and legal forward/inverse assertions, matching all22 application/four backend hashes; final Hub comparison61 checks. The full/staged suites predate only the final row/text/paint changes; final affected checks cover those changes. All are agent/controlled native evidence, not human solves or hundreds of independent user scenarios. See docs/reviews/keyboard-dock-20260915.md and native-baseline/instrument-launch-a156c3e260d97336/verification.json.

Actual UIA verified full259800-slot/1200-generator mapping, the original Current/Next and embedded keyboard. Temporary empty post-input UIA output recovered through supported window re-selection; process remained responsive. GDI controls were inspected at1000x650 and1280x720. Host evidence is1280x720/DPI96;2560x1600 remains clamped, no high-DPI/RTX4070/input-to-GPU timing claim. Native command adoption still often takes about0.66–1.51s including fixture polling.

Figma existing file now contains an editable keyboard/inline-editor vector study at node4:2. Lucid's existing workflow is open, but first-use role/frequency questionnaire requires the user's own input; the planned branch is ready in docs/specs/keyboard-dock-lucid.mmd. No Lucid edit this cycle or user profile answer was fabricated. Shared memory/guide/handoff updated. No unresolved reviewer disagreement in the current scope; next work must address remaining frame/reference/endgame workflows and latency, not treat this keyboard iteration as a completed0.4 product.

## 2026-09-16 — actual native screen recording, no application changes

Delivered a13:08.4,1920x1080 silent workbench recording with214 optional English event captions/chapters, two untouched raw segments, eight decoded frames and explicit evidence scope. Output: desktop project outputs/magic600-workbench-recording-20260916. Combined SHA256 ad21b999b1f4d5b2f7e7e353838e6578cf1f7c136de5fc448fe88319af394ade.

Added only isolated recording/test helpers: tests/NativeRecordingDemo.cs, record_native_demo.py, package_recording.py and bundle_native_recordings.py. Current a15622 native/four backend sources were hash-checked and left unchanged. The recording driver is a separate executable, operates existing native controls/commands and uses fresh Sessions; it does not add solver behavior. Explicit E1 two insertions, protected block, Next, finite preparation/cleanup, Macro Base, stale/cancel, worksheets/checkpoints,1/2/5/20-cell, Local/Global/Puzzle and filters were recorded. The supplement covers actual F11 Apply, Commands/empty search, foreign banks, Next management and compound Local filters.

Main native-20260916-035030 completed116 state/control assertions; supplement native-20260916-040228 completed35. Both are in-process agent checks, not human or physical-key trials. Main driver snapshot is archived with its exact source; supplementary driver fixes awaiting modal callback completion for serialized pacing/errors. Complete video decode passed23652frames; both indexed segment video streams match their raw payload hashes. No speed-up, generated frames or UI replacement. Encoded30fps contains repeated capture frames and is not a renderer/input-latency result.

Capture diagnosis: HWND grab omitted modals; a logical-size desktop crop missed the right/bottom of the physical144-DPI desktop. These probes were rejected. Final gdigrab desktop recording covers1920x1080 while native reports1280x720 logical/client1264x681/GDI96. Environment reports Intel HD620 and AppliedDPI144, not RTX evidence. Long tooltips and actual waits remain visible and documented. The computer-use screenshot endpoint still reports SetIsBorderRequired E_NOINTERFACE; this was a capability error, not an approval refusal. FFmpeg7.1 was configured only inside recording-tools through imageio-ffmpeg0.6.0; no OBS/global settings changed.

Independent source review led to capture exit monitoring, guaranteed owned-process cleanup and strict caption intervals. Independent final frame review confirmed complete window/modal capture, block/current/protection and saved F11 states, with no extra delivery blocker. Existing tooltip obstruction/scaling blur are preserved product limitations. No unrelated application edits or personal-session changes; owned windows exited normally. G1/G2 remain Pending. Next implementation work retains the existing frame/reference/endgame and interaction priorities; footage does not approve formal integration.

## 2026-09-16 — CurrentScore, exact reason objects and native review continuity

The selected complete operation now uses the accepted endgame-aware CurrentScore in the existing Solve Protection page. Mathematical effects, goals, reasons and protection share one ReviewContext. Missing noncanonical-reference evidence remains Unknown; scores neither stage nor execute. Independent review fixed generic-reference bypass, mismatched Before/After frame certificates, wrong Current evidence for block/orbit goals, and lost Strict-prefix conflict objects for cancelling operations. Exact first-prefix positions are taken from the existing prefix simulation, not inferred from net support.

Reason inspection resolves canonical objects, accepts a member distinct from its cycle anchor and can inspect an unchanged position without adding a false loop. Actual-screen review exposed Solve overlay and oversized hover obstruction; inspection now hides only that supporting window and can return to its retained page, while tooltips keep concise mathematical labels and action hints. The Functions toggle is preserved and reason keys display the existing onscreen symbols.

Evidence: helper11 and independent4 real-model checks with declared source/model bindings; nine related cycle checks;23 context/intent/integration checks passed but inline runner binding was not captured and is not backfilled. Final native postapproval-g2-20260916-223353 passed54 checks, executable b6b5068c24ffbcc554ff068f32890757a6c1622a27f85d005f143a4a6fdc5262, inputs unchanged. Actual desktop captures verify the bounded scene at1920x1080 output/1280x720 logical workspace, compact Solve780x570. Agent-operated legal fixtures, not human full solves. Earlier white surfaces were not reproduced; this does not establish their cause.

No unresolved mathematical disagreement remains in this bounded change. Noncanonical reference variants, position-only protection integration, all35 usable endgames, dynamic library applicability and final continuous acceptance remain open. Entire expanded0.4 estimated65% (60–70%), not test coverage. Final guide/package/video remain undelivered. Review details: docs/reviews/current-score-integration-20260916.md; current evidence index and HANDOFF updated.
