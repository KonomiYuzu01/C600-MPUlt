# Deployment plan and verification

1. Inspect proposal, memory, source repository, installed Codex and current official API/configuration schemas. Verify actual paths and boundaries.
2. Author seven project roles and custom agent definitions; connect AGENTS.md to the operating model. Verify syntax, links and Codex strict configuration parsing.
3. Implement verification and worktree entry points. Verify dry-run plans, failure propagation and isolated fixture worktree behavior. Do not start shared native windows during ongoing development.
4. Install the minimal API controller. Verify the actual SDK request shapes offline, local state handling and missing-key failure. Perform a live planning smoke test only when the user supplies local API access.
5. Install the same reviewed infrastructure into the actual source repository with a file allowlist. Add pull-request/manual CI. Verify installed hashes and preserve existing source changes.
6. Record evidence and exact outstanding credential/GitHub activation steps. Use an independent review before reporting completion.
